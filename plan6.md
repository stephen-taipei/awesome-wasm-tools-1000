# Plan 6: PDF 文件工具 (PDF-001 ~ PDF-020)

> 共 120 個工具，本檔案包含 PDF-001 ~ PDF-020

---

## PDF-001 PDF 合併

| 欄位 | 內容 |
|------|------|
| **用途** | 合併多個 PDF 檔案 |
| **技術重點** | 頁面順序、書籤合併、元資料處理 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳多個 PDF → 2. 排序 → 3. 合併 → 4. 下載 |
| **輸入格式** | 多個 PDF |
| **輸出格式** | 單一 PDF |
| **效能需求** | 記憶體：總大小 x2、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-002 PDF 分割

| 欄位 | 內容 |
|------|------|
| **用途** | 將 PDF 分割為多個檔案 |
| **技術重點** | 按頁/範圍/書籤分割 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇分割方式 → 3. 分割 → 4. 打包下載 |
| **輸入格式** | PDF |
| **輸出格式** | 多個 PDF |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-003 PDF 頁面提取

| 欄位 | 內容 |
|------|------|
| **用途** | 從 PDF 中提取指定頁面 |
| **技術重點** | 頁面選擇、範圍指定 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 預覽頁面 → 3. 選擇頁面 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（選定頁面） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-004 PDF 頁面刪除

| 欄位 | 內容 |
|------|------|
| **用途** | 從 PDF 中刪除指定頁面 |
| **技術重點** | 頁面移除、重新編號 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇刪除頁面 → 3. 刪除 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（已刪除頁面） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-005 PDF 頁面重排

| 欄位 | 內容 |
|------|------|
| **用途** | 重新排列 PDF 頁面順序 |
| **技術重點** | 拖放排序、預覽、批次操作 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 拖放排序 → 3. 儲存 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（重排後） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-006 PDF 旋轉

| 欄位 | 內容 |
|------|------|
| **用途** | 旋轉 PDF 頁面 |
| **技術重點** | 90/180/270 度旋轉、單頁/全部 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇頁面 → 3. 選擇角度 → 4. 旋轉 → 5. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（旋轉後） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-007 PDF 壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 壓縮 PDF 檔案大小 |
| **技術重點** | 圖片壓縮、字型子集化、物件壓縮 |
| **WASM 模組** | ghostscript-wasm (~10 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇壓縮等級 → 3. 壓縮 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（壓縮後） |
| **效能需求** | 記憶體：PDF 大小 x3、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-008 PDF 轉圖片

| 欄位 | 內容 |
|------|------|
| **用途** | 將 PDF 頁面轉換為圖片 |
| **技術重點** | 解析度設定、格式選擇、批次轉換 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇頁面 → 3. 設定參數 → 4. 轉換 → 5. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PNG/JPG |
| **效能需求** | 記憶體：依解析度、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-009 圖片轉 PDF

| 欄位 | 內容 |
|------|------|
| **用途** | 將圖片合併為 PDF |
| **技術重點** | 頁面大小、邊距、圖片適配 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳圖片 → 2. 排序 → 3. 設定頁面 → 4. 建立 PDF |
| **輸入格式** | PNG/JPG/JPEG |
| **輸出格式** | PDF |
| **效能需求** | 記憶體：總圖片大小、處理時間：依圖片數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-010 PDF 轉 Word

| 欄位 | 內容 |
|------|------|
| **用途** | 將 PDF 轉換為 Word 文件 |
| **技術重點** | 文字提取、版面保留、表格識別 |
| **WASM 模組** | pdfjs-wasm + docx-wasm (~5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 轉換 → 3. 下載 DOCX |
| **輸入格式** | PDF |
| **輸出格式** | DOCX |
| **效能需求** | 記憶體：PDF 大小 x3、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-011 Word 轉 PDF

| 欄位 | 內容 |
|------|------|
| **用途** | 將 Word 文件轉換為 PDF |
| **技術重點** | OOXML 解析、排版渲染 |
| **WASM 模組** | docx-wasm + pdf-lib-wasm (~4 MB) |
| **執行流程** | 1. 上傳 DOCX → 2. 轉換 → 3. 下載 PDF |
| **輸入格式** | DOCX |
| **輸出格式** | PDF |
| **效能需求** | 記憶體：檔案大小 x3、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-012 PDF 轉 Excel

| 欄位 | 內容 |
|------|------|
| **用途** | 從 PDF 提取表格到 Excel |
| **技術重點** | 表格偵測、格線識別、資料提取 |
| **WASM 模組** | pdfjs-wasm + xlsx-wasm (~6 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 偵測表格 → 3. 提取 → 4. 下載 XLSX |
| **輸入格式** | PDF |
| **輸出格式** | XLSX |
| **效能需求** | 記憶體：PDF 大小 x3、處理時間：依複雜度 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-013 Excel 轉 PDF

| 欄位 | 內容 |
|------|------|
| **用途** | 將 Excel 試算表轉換為 PDF |
| **技術重點** | 試算表渲染、分頁、列印區域 |
| **WASM 模組** | xlsx-wasm + pdf-lib-wasm (~5 MB) |
| **執行流程** | 1. 上傳 XLSX → 2. 設定範圍 → 3. 轉換 → 4. 下載 PDF |
| **輸入格式** | XLSX |
| **輸出格式** | PDF |
| **效能需求** | 記憶體：檔案大小 x3、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-014 PDF 轉 PowerPoint

| 欄位 | 內容 |
|------|------|
| **用途** | 將 PDF 轉換為 PowerPoint |
| **技術重點** | 頁面轉投影片、圖片提取 |
| **WASM 模組** | pdfjs-wasm + pptx-wasm (~5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 轉換 → 3. 下載 PPTX |
| **輸入格式** | PDF |
| **輸出格式** | PPTX |
| **效能需求** | 記憶體：PDF 大小 x3、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-015 PowerPoint 轉 PDF

| 欄位 | 內容 |
|------|------|
| **用途** | 將 PowerPoint 轉換為 PDF |
| **技術重點** | 投影片渲染、動畫移除 |
| **WASM 模組** | pptx-wasm + pdf-lib-wasm (~5 MB) |
| **執行流程** | 1. 上傳 PPTX → 2. 轉換 → 3. 下載 PDF |
| **輸入格式** | PPTX |
| **輸出格式** | PDF |
| **效能需求** | 記憶體：檔案大小 x3、處理時間：依投影片數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-016 HTML 轉 PDF

| 欄位 | 內容 |
|------|------|
| **用途** | 將 HTML 頁面轉換為 PDF |
| **技術重點** | CSS 渲染、分頁控制、連結保留 |
| **WASM 模組** | puppeteer-wasm (~30 MB) |
| **執行流程** | 1. 輸入 HTML → 2. 設定頁面 → 3. 轉換 → 4. 下載 |
| **輸入格式** | HTML |
| **輸出格式** | PDF |
| **效能需求** | 記憶體：高、處理時間：依複雜度 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-017 PDF 轉 HTML

| 欄位 | 內容 |
|------|------|
| **用途** | 將 PDF 轉換為 HTML |
| **技術重點** | 版面保留、CSS 生成、圖片提取 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 轉換 → 3. 預覽 → 4. 下載 HTML |
| **輸入格式** | PDF |
| **輸出格式** | HTML + CSS + 圖片 |
| **效能需求** | 記憶體：PDF 大小 x3、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-018 PDF 轉 Text

| 欄位 | 內容 |
|------|------|
| **用途** | 從 PDF 提取純文字 |
| **技術重點** | 文字流提取、閱讀順序、段落識別 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 提取 → 3. 編輯/複製 |
| **輸入格式** | PDF |
| **輸出格式** | TXT |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-019 Markdown 轉 PDF

| 欄位 | 內容 |
|------|------|
| **用途** | 將 Markdown 轉換為 PDF |
| **技術重點** | Markdown 解析、樣式主題、程式碼高亮 |
| **WASM 模組** | marked-wasm + pdf-lib-wasm (~2 MB) |
| **執行流程** | 1. 輸入 Markdown → 2. 選擇樣式 → 3. 轉換 → 4. 下載 |
| **輸入格式** | Markdown |
| **輸出格式** | PDF |
| **效能需求** | 記憶體：中等、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-020 PDF OCR

| 欄位 | 內容 |
|------|------|
| **用途** | 對掃描 PDF 進行 OCR |
| **技術重點** | 影像 OCR、文字圖層添加、可搜尋 PDF |
| **WASM 模組** | tesseract-wasm + pdfjs-wasm (~18 MB) |
| **執行流程** | 1. 上傳掃描 PDF → 2. OCR → 3. 生成可搜尋 PDF |
| **輸入格式** | PDF（掃描） |
| **輸出格式** | PDF（可搜尋） |
| **效能需求** | 記憶體：高、處理時間：較長 |
| **進度狀態** | ⬜ 待開發 |

---

## 下一個檔案

PDF-021 ~ PDF-040 請參閱 [plan6-2.md](plan6-2.md)
