# Plan 3-4: 壓縮轉檔工具 (CMP-061 ~ CMP-080)

> 共 120 個工具，本檔案包含 CMP-061 ~ CMP-080

---

## CMP-061 PAX 解開

| 欄位 | 內容 |
|------|------|
| **用途** | 解開 POSIX PAX 格式檔案 |
| **技術重點** | PAX 格式解析、擴展標頭支援 |
| **WASM 模組** | libarchive-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PAX → 2. 解開 → 3. 下載 |
| **輸入格式** | PAX |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：PAX 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-062 SHAR 解開

| 欄位 | 內容 |
|------|------|
| **用途** | 解開 Shell Archive 檔案 |
| **技術重點** | SHAR 腳本解析、uuencode 解碼 |
| **WASM 模組** | libarchive-wasm (~3 MB) |
| **執行流程** | 1. 上傳 SHAR → 2. 解開 → 3. 下載 |
| **輸入格式** | SHAR |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：SHAR 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-063 Z 壓縮解壓

| 欄位 | 內容 |
|------|------|
| **用途** | 解壓 Unix compress (.Z) 檔案 |
| **技術重點** | LZW 解壓 |
| **WASM 模組** | libarchive-wasm (~3 MB) |
| **執行流程** | 1. 上傳 .Z → 2. 解壓 → 3. 下載 |
| **輸入格式** | Z |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：Z 大小 x3、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-064 LZIP 壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 將檔案壓縮為 LZIP 格式 |
| **技術重點** | LZMA 壓縮、錯誤復原支援 |
| **WASM 模組** | lzip-wasm (~400 KB) |
| **執行流程** | 1. 選擇檔案 → 2. 壓縮 → 3. 下載 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | LZ |
| **效能需求** | 記憶體：檔案大小 x2、處理時間：中等 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-065 LZIP 解壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 解壓 LZIP 檔案 |
| **技術重點** | LZMA 解壓、錯誤修復 |
| **WASM 模組** | lzip-wasm (~400 KB) |
| **執行流程** | 1. 上傳 .lz → 2. 解壓 → 3. 下載 |
| **輸入格式** | LZ |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：LZ 大小 x3、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-066 LRZIP 壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 將大檔案壓縮為 LRZIP 格式（超高壓縮比） |
| **技術重點** | rzip + LZMA、長距離匹配 |
| **WASM 模組** | lrzip-wasm (~1 MB) |
| **執行流程** | 1. 選擇檔案 → 2. 壓縮 → 3. 下載 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | LRZ |
| **效能需求** | 記憶體：高、處理時間：長 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-067 LRZIP 解壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 解壓 LRZIP 檔案 |
| **技術重點** | LRZIP 解壓 |
| **WASM 模組** | lrzip-wasm (~1 MB) |
| **執行流程** | 1. 上傳 .lrz → 2. 解壓 → 3. 下載 |
| **輸入格式** | LRZ |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：高、處理時間：中等 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-068 ZPAQ 壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 將檔案壓縮為 ZPAQ 格式（極高壓縮比） |
| **技術重點** | 上下文混合、增量備份 |
| **WASM 模組** | zpaq-wasm (~2 MB) |
| **執行流程** | 1. 選擇檔案 → 2. 選擇等級 → 3. 壓縮 → 4. 下載 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | ZPAQ |
| **效能需求** | 記憶體：極高、處理時間：極長 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-069 ZPAQ 解壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 解壓 ZPAQ 檔案 |
| **技術重點** | ZPAQ 解壓、版本支援 |
| **WASM 模組** | zpaq-wasm (~2 MB) |
| **執行流程** | 1. 上傳 .zpaq → 2. 解壓 → 3. 下載 |
| **輸入格式** | ZPAQ |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：高、處理時間：中等 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-070 ACE 解壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 解壓 ACE 壓縮檔 |
| **技術重點** | ACE 格式解析（只讀） |
| **WASM 模組** | unace-wasm (~300 KB) |
| **執行流程** | 1. 上傳 ACE → 2. 解壓 → 3. 下載 |
| **輸入格式** | ACE |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：ACE 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-071 ARJ 解壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 解壓 ARJ 壓縮檔 |
| **技術重點** | ARJ 格式解析 |
| **WASM 模組** | unarj-wasm (~200 KB) |
| **執行流程** | 1. 上傳 ARJ → 2. 解壓 → 3. 下載 |
| **輸入格式** | ARJ |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：ARJ 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-072 LHA/LZH 解壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 解壓 LHA/LZH 壓縮檔 |
| **技術重點** | LHA 格式解析 |
| **WASM 模組** | lha-wasm (~250 KB) |
| **執行流程** | 1. 上傳 LHA/LZH → 2. 解壓 → 3. 下載 |
| **輸入格式** | LHA, LZH |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：檔案大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-073 ZOO 解壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 解壓 ZOO 壓縮檔 |
| **技術重點** | ZOO 格式解析 |
| **WASM 模組** | zoo-wasm (~150 KB) |
| **執行流程** | 1. 上傳 ZOO → 2. 解壓 → 3. 下載 |
| **輸入格式** | ZOO |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：ZOO 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-074 ARC 解壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 解壓 ARC 壓縮檔 |
| **技術重點** | ARC 格式解析 |
| **WASM 模組** | arc-wasm (~150 KB) |
| **執行流程** | 1. 上傳 ARC → 2. 解壓 → 3. 下載 |
| **輸入格式** | ARC |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：ARC 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-075 SIT/SITX 解壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 解壓 StuffIt 壓縮檔 |
| **技術重點** | StuffIt 格式解析 |
| **WASM 模組** | unstuff-wasm (~500 KB) |
| **執行流程** | 1. 上傳 SIT/SITX → 2. 解壓 → 3. 下載 |
| **輸入格式** | SIT, SITX |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：檔案大小 x2、處理時間：中等 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-076 壓縮檔密碼破解

| 欄位 | 內容 |
|------|------|
| **用途** | 嘗試破解簡單密碼的壓縮檔（字典攻擊） |
| **技術重點** | 字典攻擊、暴力破解（短密碼）、合法用途 |
| **WASM 模組** | zip-wasm (~800 KB) |
| **執行流程** | 1. 上傳加密壓縮檔 → 2. 選擇字典 → 3. 嘗試破解 |
| **輸入格式** | ZIP (加密) |
| **輸出格式** | 密碼（如成功） |
| **效能需求** | 記憶體：中等、處理時間：依密碼複雜度 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-077 壓縮檔加密轉換

| 欄位 | 內容 |
|------|------|
| **用途** | 為無密碼壓縮檔添加密碼保護 |
| **技術重點** | 解壓 + 加密重壓 |
| **WASM 模組** | zip-wasm (~800 KB) |
| **執行流程** | 1. 上傳壓縮檔 → 2. 設定密碼 → 3. 加密 → 4. 下載 |
| **輸入格式** | ZIP |
| **輸出格式** | ZIP (加密) |
| **效能需求** | 記憶體：檔案大小 x3、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-078 壓縮檔解密轉換

| 欄位 | 內容 |
|------|------|
| **用途** | 將加密壓縮檔轉為無密碼版本 |
| **技術重點** | 解密 + 重壓（不加密） |
| **WASM 模組** | zip-wasm (~800 KB) |
| **執行流程** | 1. 上傳加密壓縮檔 → 2. 輸入密碼 → 3. 移除加密 → 4. 下載 |
| **輸入格式** | ZIP (加密) |
| **輸出格式** | ZIP (無密碼) |
| **效能需求** | 記憶體：檔案大小 x3、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-079 壓縮檔密碼變更

| 欄位 | 內容 |
|------|------|
| **用途** | 變更加密壓縮檔的密碼 |
| **技術重點** | 解密 + 新密碼加密 |
| **WASM 模組** | zip-wasm (~800 KB) |
| **執行流程** | 1. 上傳加密檔 → 2. 輸入舊密碼 → 3. 設定新密碼 → 4. 下載 |
| **輸入格式** | ZIP (加密) |
| **輸出格式** | ZIP (新密碼) |
| **效能需求** | 記憶體：檔案大小 x3、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-080 壓縮檔結構編輯

| 欄位 | 內容 |
|------|------|
| **用途** | 編輯壓縮檔內部結構（新增/刪除/重命名檔案） |
| **技術重點** | 部分更新、目錄操作、無需完全解壓 |
| **WASM 模組** | zip-wasm (~800 KB) |
| **執行流程** | 1. 上傳壓縮檔 → 2. 編輯結構 → 3. 儲存 → 4. 下載 |
| **輸入格式** | ZIP |
| **輸出格式** | ZIP |
| **效能需求** | 記憶體：依操作、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## 下一個檔案

CMP-081 ~ CMP-100 請參閱 [plan3-5.md](plan3-5.md)
