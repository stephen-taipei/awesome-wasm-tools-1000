# Plan 4-5: 加密解密工具 (CRY-081 ~ CRY-100)

> 共 130 個工具，本檔案包含 CRY-081 ~ CRY-100

---

## CRY-081 2FA QR Code 生成

| 欄位 | 內容 |
|------|------|
| **用途** | 生成 2FA 設定用 QR Code |
| **技術重點** | otpauth:// URI、QR Code 生成 |
| **WASM 模組** | qrcode-wasm (~200 KB) |
| **執行流程** | 1. 輸入帳號資訊 → 2. 生成密鑰 → 3. 建立 QR Code → 4. 下載 |
| **輸入格式** | 帳號名稱、發行者 |
| **輸出格式** | QR Code 圖片、密鑰文字 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-082 比特幣地址生成

| 欄位 | 內容 |
|------|------|
| **用途** | 生成比特幣錢包地址 |
| **技術重點** | secp256k1、WIF 格式、Base58Check、Bech32 |
| **WASM 模組** | secp256k1-wasm (~400 KB) |
| **執行流程** | 1. 生成私鑰 → 2. 計算公鑰 → 3. 生成地址 → 4. 下載 |
| **輸入格式** | 無（或匯入私鑰） |
| **輸出格式** | 私鑰 (WIF)、地址 (P2PKH/P2WPKH) |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-083 以太坊地址生成

| 欄位 | 內容 |
|------|------|
| **用途** | 生成以太坊錢包地址 |
| **技術重點** | secp256k1、Keccak-256、Checksum 地址 |
| **WASM 模組** | secp256k1-wasm (~400 KB) |
| **執行流程** | 1. 生成私鑰 → 2. 計算公鑰 → 3. 生成地址 → 4. 下載 |
| **輸入格式** | 無（或匯入私鑰） |
| **輸出格式** | 私鑰、地址 (0x...) |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-084 BIP39 助記詞生成

| 欄位 | 內容 |
|------|------|
| **用途** | 生成 BIP39 助記詞 |
| **技術重點** | 熵生成、詞表選擇、校驗詞計算 |
| **WASM 模組** | bip39-wasm (~300 KB) |
| **執行流程** | 1. 選擇詞數 → 2. 選擇語言 → 3. 生成 → 4. 顯示 |
| **輸入格式** | 參數設定 |
| **輸出格式** | 12/15/18/21/24 詞助記詞 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-085 BIP39 助記詞驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證 BIP39 助記詞有效性 |
| **技術重點** | 詞表檢查、校驗詞驗證 |
| **WASM 模組** | bip39-wasm (~300 KB) |
| **執行流程** | 1. 輸入助記詞 → 2. 驗證 → 3. 顯示結果 |
| **輸入格式** | 助記詞 |
| **輸出格式** | 驗證結果 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-086 BIP32 HD 金鑰衍生

| 欄位 | 內容 |
|------|------|
| **用途** | 從助記詞衍生 HD 金鑰 |
| **技術重點** | BIP32/44/49/84 路徑、xpub/xprv |
| **WASM 模組** | bip32-wasm (~400 KB) |
| **執行流程** | 1. 輸入助記詞 → 2. 選擇衍生路徑 → 3. 衍生金鑰 → 4. 顯示 |
| **輸入格式** | 助記詞、衍生路徑 |
| **輸出格式** | 私鑰、公鑰、地址 |
| **效能需求** | 記憶體：低、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-087 訊息簽章（區塊鏈）

| 欄位 | 內容 |
|------|------|
| **用途** | 使用區塊鏈私鑰簽署訊息 |
| **技術重點** | Bitcoin/Ethereum 訊息簽章、recoverable signature |
| **WASM 模組** | secp256k1-wasm (~400 KB) |
| **執行流程** | 1. 輸入私鑰 → 2. 輸入訊息 → 3. 簽章 → 4. 顯示 |
| **輸入格式** | 私鑰、訊息 |
| **輸出格式** | 簽章 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-088 訊息驗證（區塊鏈）

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證區塊鏈訊息簽章 |
| **技術重點** | 簽章驗證、地址恢復 |
| **WASM 模組** | secp256k1-wasm (~400 KB) |
| **執行流程** | 1. 輸入訊息 → 2. 輸入簽章 → 3. 輸入地址 → 4. 驗證 |
| **輸入格式** | 訊息、簽章、地址 |
| **輸出格式** | 驗證結果 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-089 Shamir 秘密分享

| 欄位 | 內容 |
|------|------|
| **用途** | 將秘密分割為多個份額 |
| **技術重點** | Shamir's Secret Sharing、閾值方案 |
| **WASM 模組** | sss-wasm (~200 KB) |
| **執行流程** | 1. 輸入秘密 → 2. 設定閾值 → 3. 設定份額數 → 4. 分割 |
| **輸入格式** | 秘密（文字/金鑰） |
| **輸出格式** | 多個份額 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-090 Shamir 秘密重建

| 欄位 | 內容 |
|------|------|
| **用途** | 從份額重建秘密 |
| **技術重點** | 拉格朗日插值、份額驗證 |
| **WASM 模組** | sss-wasm (~200 KB) |
| **執行流程** | 1. 輸入份額 → 2. 重建 → 3. 顯示秘密 |
| **輸入格式** | 足夠數量的份額 |
| **輸出格式** | 原始秘密 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-091 檔案加密（對稱）

| 欄位 | 內容 |
|------|------|
| **用途** | 使用密碼加密檔案 |
| **技術重點** | Argon2 + AES-GCM、加密格式、元資料保護 |
| **WASM 模組** | argon2-wasm + crypto-wasm (~1.1 MB) |
| **執行流程** | 1. 上傳檔案 → 2. 輸入密碼 → 3. 加密 → 4. 下載 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | 加密檔案 (.enc) |
| **效能需求** | 記憶體：檔案大小 x2、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-092 檔案解密（對稱）

| 欄位 | 內容 |
|------|------|
| **用途** | 使用密碼解密檔案 |
| **技術重點** | 格式解析、金鑰衍生、解密驗證 |
| **WASM 模組** | argon2-wasm + crypto-wasm (~1.1 MB) |
| **執行流程** | 1. 上傳加密檔案 → 2. 輸入密碼 → 3. 解密 → 4. 下載 |
| **輸入格式** | 加密檔案 (.enc) |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：檔案大小 x2、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-093 Age 加密

| 欄位 | 內容 |
|------|------|
| **用途** | 使用 Age 格式加密檔案 |
| **技術重點** | Age 協議、X25519、scrypt 密碼 |
| **WASM 模組** | age-wasm (~500 KB) |
| **執行流程** | 1. 上傳檔案 → 2. 選擇加密方式 → 3. 加密 → 4. 下載 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | Age 加密檔案 |
| **效能需求** | 記憶體：檔案大小 x2、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-094 Age 解密

| 欄位 | 內容 |
|------|------|
| **用途** | 解密 Age 格式檔案 |
| **技術重點** | Age 解密、金鑰/密碼識別 |
| **WASM 模組** | age-wasm (~500 KB) |
| **執行流程** | 1. 上傳加密檔案 → 2. 輸入金鑰/密碼 → 3. 解密 → 4. 下載 |
| **輸入格式** | Age 加密檔案 |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：檔案大小 x2、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-095 Age 金鑰生成

| 欄位 | 內容 |
|------|------|
| **用途** | 生成 Age 金鑰對 |
| **技術重點** | X25519 金鑰生成、Age 格式 |
| **WASM 模組** | age-wasm (~500 KB) |
| **執行流程** | 1. 生成金鑰 → 2. 顯示公私鑰 → 3. 下載 |
| **輸入格式** | 無 |
| **輸出格式** | Age 公鑰、Age 私鑰 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-096 隨機數生成器

| 欄位 | 內容 |
|------|------|
| **用途** | 生成密碼學安全隨機數 |
| **技術重點** | CSPRNG、多格式輸出 |
| **WASM 模組** | 原生 Web Crypto API |
| **執行流程** | 1. 設定長度 → 2. 選擇格式 → 3. 生成 → 4. 複製 |
| **輸入格式** | 長度、格式設定 |
| **輸出格式** | Hex/Base64/Bytes |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-097 UUID 生成器

| 欄位 | 內容 |
|------|------|
| **用途** | 生成各版本 UUID |
| **技術重點** | UUID v1/v4/v5/v7、命名空間 |
| **WASM 模組** | 原生 Web Crypto API |
| **執行流程** | 1. 選擇版本 → 2. 設定參數 → 3. 生成 → 4. 複製 |
| **輸入格式** | 版本、命名空間（v5） |
| **輸出格式** | UUID 字串 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-098 ULID 生成器

| 欄位 | 內容 |
|------|------|
| **用途** | 生成可排序的唯一 ID |
| **技術重點** | ULID 規格、時間戳 + 隨機、Crockford Base32 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 生成 → 2. 顯示 → 3. 複製 |
| **輸入格式** | 無 |
| **輸出格式** | ULID 字串 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-099 Nanoid 生成器

| 欄位 | 內容 |
|------|------|
| **用途** | 生成短唯一 ID |
| **技術重點** | Nanoid、自訂字母表、URL 安全 |
| **WASM 模組** | 原生 Web Crypto API |
| **執行流程** | 1. 設定長度 → 2. 選擇字母表 → 3. 生成 → 4. 複製 |
| **輸入格式** | 長度、字母表 |
| **輸出格式** | Nanoid 字串 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-100 Snowflake ID 生成器

| 欄位 | 內容 |
|------|------|
| **用途** | 生成 Twitter Snowflake 風格 ID |
| **技術重點** | 時間戳 + 機器 ID + 序號、64 位元 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 設定機器 ID → 2. 生成 → 3. 顯示詳細資訊 |
| **輸入格式** | 機器 ID |
| **輸出格式** | Snowflake ID |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## 下一個檔案

CRY-101 ~ CRY-120 請參閱 [plan4-6.md](plan4-6.md)
