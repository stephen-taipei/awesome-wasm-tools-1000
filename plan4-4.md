# Plan 4-4: 加密解密工具 (CRY-061 ~ CRY-080)

> 共 130 個工具，本檔案包含 CRY-061 ~ CRY-080

---

## CRY-061 PGP 簽章驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證 PGP 數位簽章 |
| **技術重點** | OpenPGP 驗證、公鑰匹配 |
| **WASM 模組** | openpgp-wasm (~2 MB) |
| **執行流程** | 1. 上傳公鑰 → 2. 上傳內容 → 3. 上傳簽章 → 4. 驗證 |
| **輸入格式** | 公鑰、訊息/檔案、簽章 |
| **輸出格式** | 驗證結果 |
| **效能需求** | 記憶體：低、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-062 SSH 金鑰生成

| 欄位 | 內容 |
|------|------|
| **用途** | 生成 SSH 金鑰對 |
| **技術重點** | RSA/Ed25519/ECDSA、OpenSSH 格式、密碼保護 |
| **WASM 模組** | sshpk-wasm (~600 KB) |
| **執行流程** | 1. 選擇演算法 → 2. 設定密碼 → 3. 生成 → 4. 下載 |
| **輸入格式** | 參數設定 |
| **輸出格式** | id_rsa/id_ed25519、*.pub |
| **效能需求** | 記憶體：低、處理時間：數秒 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-063 SSH 公鑰指紋

| 欄位 | 內容 |
|------|------|
| **用途** | 計算 SSH 公鑰指紋 |
| **技術重點** | MD5/SHA256 指紋、多格式支援 |
| **WASM 模組** | sshpk-wasm (~600 KB) |
| **執行流程** | 1. 上傳公鑰 → 2. 計算指紋 → 3. 顯示結果 |
| **輸入格式** | SSH 公鑰 |
| **輸出格式** | 指紋（MD5/SHA256） |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-064 SSH 金鑰格式轉換

| 欄位 | 內容 |
|------|------|
| **用途** | 轉換 SSH 金鑰格式 |
| **技術重點** | OpenSSH ↔ PEM ↔ PPK 轉換 |
| **WASM 模組** | sshpk-wasm (~600 KB) |
| **執行流程** | 1. 上傳金鑰 → 2. 選擇目標格式 → 3. 轉換 → 4. 下載 |
| **輸入格式** | SSH 金鑰（各格式） |
| **輸出格式** | 轉換後金鑰 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-065 X.509 憑證生成

| 欄位 | 內容 |
|------|------|
| **用途** | 生成自簽 X.509 憑證 |
| **技術重點** | X.509 v3、主體資訊、擴展欄位、有效期 |
| **WASM 模組** | pkijs-wasm (~1 MB) |
| **執行流程** | 1. 輸入主體資訊 → 2. 設定有效期 → 3. 生成金鑰 → 4. 下載 |
| **輸入格式** | 主體資訊 |
| **輸出格式** | PEM 憑證、PEM 私鑰 |
| **效能需求** | 記憶體：中等、處理時間：數秒 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-066 CSR 生成

| 欄位 | 內容 |
|------|------|
| **用途** | 生成憑證簽署請求 (CSR) |
| **技術重點** | PKCS#10、主體名稱、SAN 擴展 |
| **WASM 模組** | pkijs-wasm (~1 MB) |
| **執行流程** | 1. 輸入主體資訊 → 2. 生成金鑰 → 3. 建立 CSR → 4. 下載 |
| **輸入格式** | 主體資訊 |
| **輸出格式** | PEM CSR、PEM 私鑰 |
| **效能需求** | 記憶體：中等、處理時間：數秒 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-067 憑證資訊檢視

| 欄位 | 內容 |
|------|------|
| **用途** | 檢視 X.509 憑證詳細資訊 |
| **技術重點** | X.509 解析、擴展欄位、指紋計算 |
| **WASM 模組** | pkijs-wasm (~1 MB) |
| **執行流程** | 1. 上傳憑證 → 2. 解析 → 3. 顯示資訊 |
| **輸入格式** | PEM/DER 憑證 |
| **輸出格式** | 結構化資訊 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-068 憑證鏈驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證憑證信任鏈 |
| **技術重點** | 憑證鏈驗證、簽章驗證、過期檢查 |
| **WASM 模組** | pkijs-wasm (~1 MB) |
| **執行流程** | 1. 上傳憑證 → 2. 上傳 CA 憑證 → 3. 驗證 → 4. 顯示結果 |
| **輸入格式** | 憑證、CA 憑證 |
| **輸出格式** | 驗證結果 |
| **效能需求** | 記憶體：低、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-069 PEM ↔ DER 轉換

| 欄位 | 內容 |
|------|------|
| **用途** | 轉換憑證/金鑰格式（PEM ↔ DER） |
| **技術重點** | Base64 編解碼、ASN.1 結構 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 上傳檔案 → 2. 偵測格式 → 3. 轉換 → 4. 下載 |
| **輸入格式** | PEM 或 DER |
| **輸出格式** | DER 或 PEM |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-070 PKCS#12 建立

| 欄位 | 內容 |
|------|------|
| **用途** | 建立 PKCS#12 (.p12/.pfx) 檔案 |
| **技術重點** | PKCS#12 封裝、私鑰 + 憑證、密碼保護 |
| **WASM 模組** | pkijs-wasm (~1 MB) |
| **執行流程** | 1. 上傳私鑰 → 2. 上傳憑證 → 3. 設定密碼 → 4. 建立 → 5. 下載 |
| **輸入格式** | PEM 私鑰、PEM 憑證 |
| **輸出格式** | P12/PFX |
| **效能需求** | 記憶體：低、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-071 PKCS#12 解開

| 欄位 | 內容 |
|------|------|
| **用途** | 解開 PKCS#12 檔案提取內容 |
| **技術重點** | PKCS#12 解析、私鑰提取、憑證提取 |
| **WASM 模組** | pkijs-wasm (~1 MB) |
| **執行流程** | 1. 上傳 P12 → 2. 輸入密碼 → 3. 解開 → 4. 下載 |
| **輸入格式** | P12/PFX |
| **輸出格式** | PEM 私鑰、PEM 憑證 |
| **效能需求** | 記憶體：低、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-072 JWT 生成

| 欄位 | 內容 |
|------|------|
| **用途** | 生成 JSON Web Token |
| **技術重點** | HS256/RS256/ES256、Claims 設定、簽章 |
| **WASM 模組** | jose-wasm (~400 KB) |
| **執行流程** | 1. 設定 Header → 2. 設定 Payload → 3. 輸入金鑰 → 4. 簽章 |
| **輸入格式** | Claims JSON、金鑰 |
| **輸出格式** | JWT 字串 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-073 JWT 驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證並解碼 JWT |
| **技術重點** | 簽章驗證、過期檢查、Claims 提取 |
| **WASM 模組** | jose-wasm (~400 KB) |
| **執行流程** | 1. 輸入 JWT → 2. 輸入金鑰 → 3. 驗證 → 4. 顯示內容 |
| **輸入格式** | JWT 字串、驗證金鑰 |
| **輸出格式** | 驗證結果、解碼內容 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-074 JWT 解碼（無驗證）

| 欄位 | 內容 |
|------|------|
| **用途** | 解碼 JWT 內容（不驗證簽章） |
| **技術重點** | Base64URL 解碼、JSON 解析 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入 JWT → 2. 解碼 → 3. 顯示 Header/Payload |
| **輸入格式** | JWT 字串 |
| **輸出格式** | Header JSON、Payload JSON |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-075 JWK 生成

| 欄位 | 內容 |
|------|------|
| **用途** | 生成 JSON Web Key |
| **技術重點** | RSA/EC/對稱 JWK、金鑰用途、kid 生成 |
| **WASM 模組** | jose-wasm (~400 KB) |
| **執行流程** | 1. 選擇演算法 → 2. 設定用途 → 3. 生成 → 4. 下載 |
| **輸入格式** | 參數設定 |
| **輸出格式** | JWK JSON |
| **效能需求** | 記憶體：低、處理時間：數秒 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-076 JWK ↔ PEM 轉換

| 欄位 | 內容 |
|------|------|
| **用途** | 轉換 JWK 與 PEM 格式 |
| **技術重點** | JWK 解析、PEM 編碼、SPKI/PKCS#8 |
| **WASM 模組** | jose-wasm (~400 KB) |
| **執行流程** | 1. 上傳 JWK/PEM → 2. 轉換 → 3. 下載 |
| **輸入格式** | JWK 或 PEM |
| **輸出格式** | PEM 或 JWK |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-077 JWE 加密

| 欄位 | 內容 |
|------|------|
| **用途** | 建立加密的 JSON Web Encryption |
| **技術重點** | JWE Compact/JSON、RSA-OAEP/A256GCM |
| **WASM 模組** | jose-wasm (~400 KB) |
| **執行流程** | 1. 輸入內容 → 2. 選擇演算法 → 3. 輸入金鑰 → 4. 加密 |
| **輸入格式** | 明文、加密金鑰 |
| **輸出格式** | JWE 字串 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-078 JWE 解密

| 欄位 | 內容 |
|------|------|
| **用途** | 解密 JSON Web Encryption |
| **技術重點** | JWE 解密、金鑰匹配 |
| **WASM 模組** | jose-wasm (~400 KB) |
| **執行流程** | 1. 輸入 JWE → 2. 輸入金鑰 → 3. 解密 → 4. 顯示內容 |
| **輸入格式** | JWE 字串、解密金鑰 |
| **輸出格式** | 解密內容 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-079 TOTP 生成器

| 欄位 | 內容 |
|------|------|
| **用途** | 生成基於時間的一次性密碼 |
| **技術重點** | TOTP (RFC 6238)、30 秒週期、6-8 位數 |
| **WASM 模組** | 原生 Web Crypto API |
| **執行流程** | 1. 輸入密鑰 → 2. 自動計算 → 3. 顯示動態密碼 |
| **輸入格式** | Base32 密鑰 |
| **輸出格式** | 6/8 位數密碼 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-080 HOTP 生成器

| 欄位 | 內容 |
|------|------|
| **用途** | 生成基於計數器的一次性密碼 |
| **技術重點** | HOTP (RFC 4226)、計數器管理 |
| **WASM 模組** | 原生 Web Crypto API |
| **執行流程** | 1. 輸入密鑰 → 2. 設定計數器 → 3. 生成密碼 |
| **輸入格式** | Base32 密鑰、計數器值 |
| **輸出格式** | 6/8 位數密碼 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## 下一個檔案

CRY-081 ~ CRY-100 請參閱 [plan4-5.md](plan4-5.md)
