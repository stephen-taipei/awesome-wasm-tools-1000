# Plan 7: 編碼轉換工具 (ENC-001 ~ ENC-020)

> 共 100 個工具，本檔案包含 ENC-001 ~ ENC-020

---

## ENC-001 Base64 編碼

| 欄位 | 內容 |
|------|------|
| **用途** | 將文字/檔案編碼為 Base64 |
| **技術重點** | 標準 Base64、URL 安全 Base64 |
| **WASM 模組** | 原生 JS (btoa/atob) |
| **執行流程** | 1. 輸入文字/上傳檔案 → 2. 編碼 → 3. 複製/下載 |
| **輸入格式** | 文字、任意檔案 |
| **輸出格式** | Base64 字串 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-002 Base64 解碼

| 欄位 | 內容 |
|------|------|
| **用途** | 將 Base64 解碼為原始內容 |
| **技術重點** | 自動偵測格式、錯誤處理 |
| **WASM 模組** | 原生 JS (btoa/atob) |
| **執行流程** | 1. 輸入 Base64 → 2. 解碼 → 3. 顯示/下載 |
| **輸入格式** | Base64 字串 |
| **輸出格式** | 文字、二進位檔案 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-003 Base32 編碼

| 欄位 | 內容 |
|------|------|
| **用途** | 將文字編碼為 Base32 |
| **技術重點** | RFC 4648 標準、填充處理 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入文字 → 2. 編碼 → 3. 複製 |
| **輸入格式** | 文字 |
| **輸出格式** | Base32 字串 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-004 Base32 解碼

| 欄位 | 內容 |
|------|------|
| **用途** | 將 Base32 解碼為文字 |
| **技術重點** | 填充容錯、大小寫不敏感 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入 Base32 → 2. 解碼 → 3. 顯示 |
| **輸入格式** | Base32 字串 |
| **輸出格式** | 文字 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-005 Base16 (Hex) 編碼

| 欄位 | 內容 |
|------|------|
| **用途** | 將文字/檔案轉為十六進位 |
| **技術重點** | 大小寫選擇、分隔符設定 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入文字/上傳檔案 → 2. 轉換 → 3. 複製 |
| **輸入格式** | 文字、二進位 |
| **輸出格式** | Hex 字串 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-006 Base16 (Hex) 解碼

| 欄位 | 內容 |
|------|------|
| **用途** | 將十六進位轉為原始內容 |
| **技術重點** | 格式容錯、自動清理 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入 Hex → 2. 解碼 → 3. 顯示/下載 |
| **輸入格式** | Hex 字串 |
| **輸出格式** | 文字、二進位 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-007 Base58 編碼

| 欄位 | 內容 |
|------|------|
| **用途** | 將資料編碼為 Base58 |
| **技術重點** | Bitcoin/Flickr 字母表、無混淆字元 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入資料 → 2. 選擇字母表 → 3. 編碼 → 4. 複製 |
| **輸入格式** | 文字、二進位 |
| **輸出格式** | Base58 字串 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-008 Base58 解碼

| 欄位 | 內容 |
|------|------|
| **用途** | 將 Base58 解碼為原始內容 |
| **技術重點** | 字母表自動偵測 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入 Base58 → 2. 解碼 → 3. 顯示 |
| **輸入格式** | Base58 字串 |
| **輸出格式** | 文字、二進位 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-009 Base85 (Ascii85) 編碼

| 欄位 | 內容 |
|------|------|
| **用途** | 將資料編碼為 Base85 |
| **技術重點** | Adobe/btoa 格式、Z85 變體 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入資料 → 2. 選擇格式 → 3. 編碼 → 4. 複製 |
| **輸入格式** | 文字、二進位 |
| **輸出格式** | Base85 字串 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-010 Base85 (Ascii85) 解碼

| 欄位 | 內容 |
|------|------|
| **用途** | 將 Base85 解碼為原始內容 |
| **技術重點** | 格式自動偵測 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入 Base85 → 2. 解碼 → 3. 顯示 |
| **輸入格式** | Base85 字串 |
| **輸出格式** | 文字、二進位 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-011 URL 編碼

| 欄位 | 內容 |
|------|------|
| **用途** | 將文字進行 URL 編碼 |
| **技術重點** | 百分號編碼、保留字元、空格處理 |
| **WASM 模組** | 原生 JS (encodeURIComponent) |
| **執行流程** | 1. 輸入文字 → 2. 編碼 → 3. 複製 |
| **輸入格式** | 文字 |
| **輸出格式** | URL 編碼字串 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-012 URL 解碼

| 欄位 | 內容 |
|------|------|
| **用途** | 解碼 URL 編碼文字 |
| **技術重點** | 多次編碼處理、UTF-8 支援 |
| **WASM 模組** | 原生 JS (decodeURIComponent) |
| **執行流程** | 1. 輸入編碼文字 → 2. 解碼 → 3. 顯示 |
| **輸入格式** | URL 編碼字串 |
| **輸出格式** | 原始文字 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-013 HTML 實體編碼

| 欄位 | 內容 |
|------|------|
| **用途** | 將特殊字元轉為 HTML 實體 |
| **技術重點** | 命名實體、數字實體、全部轉換 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入文字 → 2. 選擇模式 → 3. 編碼 → 4. 複製 |
| **輸入格式** | 文字 |
| **輸出格式** | HTML 實體編碼文字 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-014 HTML 實體解碼

| 欄位 | 內容 |
|------|------|
| **用途** | 將 HTML 實體還原為字元 |
| **技術重點** | 所有實體類型支援 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入編碼文字 → 2. 解碼 → 3. 顯示 |
| **輸入格式** | HTML 實體編碼文字 |
| **輸出格式** | 原始文字 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-015 Unicode 轉義編碼

| 欄位 | 內容 |
|------|------|
| **用途** | 將文字轉為 Unicode 轉義序列 |
| **技術重點** | \uXXXX、\u{XXXXX}、HTML 數字實體 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入文字 → 2. 選擇格式 → 3. 轉換 → 4. 複製 |
| **輸入格式** | 文字 |
| **輸出格式** | Unicode 轉義字串 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-016 Unicode 轉義解碼

| 欄位 | 內容 |
|------|------|
| **用途** | 將 Unicode 轉義序列還原 |
| **技術重點** | 多種格式支援、混合內容 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入轉義序列 → 2. 解碼 → 3. 顯示 |
| **輸入格式** | Unicode 轉義字串 |
| **輸出格式** | 原始文字 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-017 Punycode 編碼

| 欄位 | 內容 |
|------|------|
| **用途** | 將國際化域名編碼為 Punycode |
| **技術重點** | IDN 標準、xn-- 前綴 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入域名 → 2. 編碼 → 3. 複製 |
| **輸入格式** | 國際化域名 |
| **輸出格式** | Punycode 域名 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-018 Punycode 解碼

| 欄位 | 內容 |
|------|------|
| **用途** | 將 Punycode 解碼為國際化域名 |
| **技術重點** | xn-- 前綴處理 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入 Punycode → 2. 解碼 → 3. 顯示 |
| **輸入格式** | Punycode 域名 |
| **輸出格式** | 國際化域名 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-019 ROT13 編碼

| 欄位 | 內容 |
|------|------|
| **用途** | 對文字進行 ROT13 置換 |
| **技術重點** | 字母輪轉、可逆加密 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入文字 → 2. 編碼 → 3. 複製 |
| **輸入格式** | 文字 |
| **輸出格式** | ROT13 編碼文字 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-020 ROT47 編碼

| 欄位 | 內容 |
|------|------|
| **用途** | 對文字進行 ROT47 置換 |
| **技術重點** | ASCII 可印刷字元輪轉 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入文字 → 2. 編碼 → 3. 複製 |
| **輸入格式** | 文字 |
| **輸出格式** | ROT47 編碼文字 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## 下一個檔案

ENC-021 ~ ENC-040 請參閱 [plan7-2.md](plan7-2.md)
