# Plan 6-6: PDF 文件工具 (PDF-101 ~ PDF-120)

> 共 120 個工具，本檔案包含 PDF-101 ~ PDF-120（最終檔案）

---

## PDF-101 PDF 文件對比報告

| 欄位 | 內容 |
|------|------|
| **用途** | 生成詳細的 PDF 對比報告 |
| **技術重點** | 逐行對比、變更追蹤、報告生成 |
| **WASM 模組** | pdfjs-wasm + diff-wasm (~4 MB) |
| **執行流程** | 1. 上傳兩個 PDF → 2. 對比分析 → 3. 生成報告 → 4. 下載 |
| **輸入格式** | 兩個 PDF |
| **輸出格式** | 對比報告 (HTML/PDF) |
| **效能需求** | 記憶體：PDF 大小 x4、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-102 PDF 版本歷史

| 欄位 | 內容 |
|------|------|
| **用途** | 追蹤 PDF 修改歷史 |
| **技術重點** | 增量更新解析、版本提取 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 解析版本 → 3. 顯示歷史 → 4. 可提取舊版 |
| **輸入格式** | PDF |
| **輸出格式** | 版本列表、舊版 PDF |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-103 PDF 資料遮蔽

| 欄位 | 內容 |
|------|------|
| **用途** | 遮蔽 PDF 中的敏感資料 |
| **技術重點** | 區域選擇、文字搜尋遮蔽、永久移除 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇/搜尋敏感區域 → 3. 遮蔽 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（已遮蔽） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-104 PDF 敏感詞檢測

| 欄位 | 內容 |
|------|------|
| **用途** | 檢測 PDF 中的敏感詞彙 |
| **技術重點** | 關鍵字搜尋、正規表達式、自訂詞庫 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 設定敏感詞 → 3. 檢測 → 4. 顯示報告 |
| **輸入格式** | PDF、敏感詞清單 |
| **輸出格式** | 檢測報告 |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-105 PDF 隱藏內容移除

| 欄位 | 內容 |
|------|------|
| **用途** | 移除 PDF 中的隱藏內容 |
| **技術重點** | 隱藏圖層、白色文字、元資料 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 偵測隱藏內容 → 3. 選擇移除 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（已清理） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-106 PDF JavaScript 移除

| 欄位 | 內容 |
|------|------|
| **用途** | 移除 PDF 中的 JavaScript |
| **技術重點** | 腳本偵測、安全移除 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 偵測 JS → 3. 移除 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（無 JavaScript） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-107 PDF 動作移除

| 欄位 | 內容 |
|------|------|
| **用途** | 移除 PDF 中的動作觸發器 |
| **技術重點** | 開啟動作、頁面動作、連結動作 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 列出動作 → 3. 選擇移除 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（動作已移除） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-108 PDF 嵌入檔案掃描

| 欄位 | 內容 |
|------|------|
| **用途** | 掃描 PDF 中的嵌入檔案 |
| **技術重點** | 附件偵測、類型識別、安全檢查 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 掃描嵌入檔 → 3. 顯示清單 → 4. 可提取/移除 |
| **輸入格式** | PDF |
| **輸出格式** | 嵌入檔案清單 |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-109 PDF 壓縮等級分析

| 欄位 | 內容 |
|------|------|
| **用途** | 分析 PDF 的壓縮效率 |
| **技術重點** | 串流壓縮分析、最佳化建議 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 分析壓縮 → 3. 顯示報告與建議 |
| **輸入格式** | PDF |
| **輸出格式** | 壓縮分析報告 |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-110 PDF 圖片壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 單獨壓縮 PDF 中的圖片 |
| **技術重點** | 圖片提取、重新壓縮、品質控制 |
| **WASM 模組** | pdfjs-wasm + image-wasm (~5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 設定壓縮選項 → 3. 壓縮圖片 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（圖片已壓縮） |
| **效能需求** | 記憶體：PDF 大小 x3、處理時間：依圖片數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-111 PDF 圖片解析度調整

| 欄位 | 內容 |
|------|------|
| **用途** | 調整 PDF 中圖片的解析度 |
| **技術重點** | DPI 設定、圖片縮放、品質保持 |
| **WASM 模組** | pdfjs-wasm + image-wasm (~5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 設定目標 DPI → 3. 處理 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（解析度已調整） |
| **效能需求** | 記憶體：PDF 大小 x3、處理時間：依圖片數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-112 PDF 目錄生成

| 欄位 | 內容 |
|------|------|
| **用途** | 自動生成 PDF 目錄頁 |
| **技術重點** | 書籤解析、頁碼對應、樣式設定 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 解析書籤 → 3. 設定樣式 → 4. 插入目錄頁 |
| **輸入格式** | PDF（含書籤） |
| **輸出格式** | PDF（含目錄） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-113 PDF 索引生成

| 欄位 | 內容 |
|------|------|
| **用途** | 生成 PDF 的詞彙索引 |
| **技術重點** | 關鍵字提取、頁碼標記、字母排序 |
| **WASM 模組** | pdfjs-wasm + pdf-lib-wasm (~4.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 提取關鍵字 → 3. 生成索引 → 4. 插入索引頁 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（含索引） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-114 PDF 參考文獻提取

| 欄位 | 內容 |
|------|------|
| **用途** | 從學術 PDF 提取參考文獻 |
| **技術重點** | 參考文獻區塊識別、格式解析、BibTeX 匯出 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 識別參考文獻 → 3. 解析 → 4. 匯出 |
| **輸入格式** | PDF（學術文件） |
| **輸出格式** | BibTeX/JSON |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-115 PDF 表格智慧提取

| 欄位 | 內容 |
|------|------|
| **用途** | 智慧識別並提取 PDF 表格 |
| **技術重點** | 表格邊界偵測、合併儲存格、多表格 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 偵測表格 → 3. 選擇提取 → 4. 匯出 |
| **輸入格式** | PDF |
| **輸出格式** | CSV/Excel/JSON |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-116 PDF 圖表提取

| 欄位 | 內容 |
|------|------|
| **用途** | 從 PDF 提取圖表圖片 |
| **技術重點** | 圖表識別、向量/點陣提取、高品質輸出 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 偵測圖表 → 3. 選擇提取 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PNG/SVG |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-117 PDF 公式提取

| 欄位 | 內容 |
|------|------|
| **用途** | 從 PDF 提取數學公式 |
| **技術重點** | 公式區域識別、LaTeX 轉換、MathML 輸出 |
| **WASM 模組** | pdfjs-wasm + mathpix-wasm (~6 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 識別公式 → 3. 轉換 → 4. 匯出 LaTeX |
| **輸入格式** | PDF |
| **輸出格式** | LaTeX/MathML |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-118 PDF 程式碼提取

| 欄位 | 內容 |
|------|------|
| **用途** | 從 PDF 提取程式碼區塊 |
| **技術重點** | 程式碼區塊識別、語法保留、格式還原 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 識別程式碼 → 3. 提取 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | 原始碼檔案 |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-119 PDF 拖放編輯器

| 欄位 | 內容 |
|------|------|
| **用途** | 視覺化拖放編輯 PDF 元素 |
| **技術重點** | 物件選取、移動縮放、圖層管理 |
| **WASM 模組** | pdf-lib-wasm + pdfjs-wasm (~4.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 視覺化編輯 → 3. 儲存 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（已編輯） |
| **效能需求** | 記憶體：PDF 大小 x3、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-120 PDF 工作流自動化

| 欄位 | 內容 |
|------|------|
| **用途** | 建立自訂 PDF 處理工作流 |
| **技術重點** | 步驟串接、條件判斷、批次處理 |
| **WASM 模組** | 依工作流步驟動態載入 |
| **執行流程** | 1. 建立工作流 → 2. 上傳 PDF → 3. 自動執行 → 4. 下載結果 |
| **輸入格式** | PDF（單個或多個） |
| **輸出格式** | 依工作流設定 |
| **效能需求** | 記憶體：依步驟、處理時間：依步驟 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF 文件工具總覽

| 分類 | 工具編號 | 數量 | 檔案 |
|------|----------|------|------|
| 基本操作 | PDF-001~006 | 6 | plan6.md |
| 格式轉換 | PDF-007~020 | 14 | plan6.md |
| 安全加密 | PDF-021~025 | 5 | plan6-2.md |
| 浮水印標記 | PDF-026~029 | 4 | plan6-2.md |
| 書籤連結 | PDF-030~032 | 3 | plan6-2.md |
| 元資料 | PDF-033~036 | 4 | plan6-2.md |
| 頁面尺寸 | PDF-037~040 | 4 | plan6-2.md |
| 排版印刷 | PDF-041~042 | 2 | plan6-3.md |
| 表單處理 | PDF-043~047 | 5 | plan6-3.md |
| 註解標記 | PDF-048~050 | 3 | plan6-3.md |
| 字型圖片 | PDF-051~059 | 9 | plan6-3.md |
| 頁面背景 | PDF-060 | 1 | plan6-3.md |
| 比較修復 | PDF-061~063 | 3 | plan6-4.md |
| 版本標準 | PDF-064~068 | 5 | plan6-4.md |
| 色彩處理 | PDF-069~071 | 3 | plan6-4.md |
| 圖層附件 | PDF-072~076 | 5 | plan6-4.md |
| 可存取性 | PDF-077~079 | 3 | plan6-4.md |
| 電子書 | PDF-080 | 1 | plan6-4.md |
| 格式轉換 2 | PDF-081~089 | 9 | plan6-5.md |
| 頁面操作 | PDF-090~092 | 3 | plan6-5.md |
| 批次處理 | PDF-093~095 | 3 | plan6-5.md |
| 預覽檢視 | PDF-096~100 | 5 | plan6-5.md |
| 對比分析 | PDF-101~102 | 2 | plan6-6.md |
| 安全清理 | PDF-103~109 | 7 | plan6-6.md |
| 壓縮最佳化 | PDF-110~111 | 2 | plan6-6.md |
| 目錄索引 | PDF-112~114 | 3 | plan6-6.md |
| 智慧提取 | PDF-115~118 | 4 | plan6-6.md |
| 編輯自動化 | PDF-119~120 | 2 | plan6-6.md |

---

## 下一個類別

編碼轉換工具 (ENC-001 ~ ENC-100) 請參閱 [plan7.md](plan7.md)
