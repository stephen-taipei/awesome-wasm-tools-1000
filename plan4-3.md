# Plan 4-3: 加密解密工具 (CRY-041 ~ CRY-060)

> 共 130 個工具，本檔案包含 CRY-041 ~ CRY-060

---

## CRY-041 Adler-32 校驗

| 欄位 | 內容 |
|------|------|
| **用途** | 計算 Adler-32 校驗值（zlib 使用） |
| **技術重點** | Adler-32 演算法、快速校驗 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入資料 → 2. 計算 → 3. 顯示校驗值 |
| **輸入格式** | 文字、任意檔案 |
| **輸出格式** | 8 字元十六進位 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-042 xxHash 雜湊

| 欄位 | 內容 |
|------|------|
| **用途** | 計算 xxHash 雜湊值（極速非加密雜湊） |
| **技術重點** | xxHash32/64/128、XXH3 |
| **WASM 模組** | xxhash-wasm (~100 KB) |
| **執行流程** | 1. 選擇變體 → 2. 輸入資料 → 3. 計算 → 4. 顯示 |
| **輸入格式** | 文字、任意檔案 |
| **輸出格式** | 十六進位雜湊值 |
| **效能需求** | 記憶體：低、處理時間：極快 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-043 MurmurHash 雜湊

| 欄位 | 內容 |
|------|------|
| **用途** | 計算 MurmurHash 雜湊值（雜湊表用） |
| **技術重點** | MurmurHash2/3、高分散性 |
| **WASM 模組** | hash-wasm (~200 KB) |
| **執行流程** | 1. 選擇版本 → 2. 輸入資料 → 3. 計算 → 4. 顯示 |
| **輸入格式** | 文字、任意檔案 |
| **輸出格式** | 十六進位雜湊值 |
| **效能需求** | 記憶體：低、處理時間：極快 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-044 CityHash 雜湊

| 欄位 | 內容 |
|------|------|
| **用途** | 計算 CityHash 雜湊值（Google 開發） |
| **技術重點** | CityHash64/128、字串最佳化 |
| **WASM 模組** | hash-wasm (~200 KB) |
| **執行流程** | 1. 輸入資料 → 2. 計算 → 3. 顯示雜湊 |
| **輸入格式** | 文字、任意檔案 |
| **輸出格式** | 十六進位雜湊值 |
| **效能需求** | 記憶體：低、處理時間：極快 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-045 FarmHash 雜湊

| 欄位 | 內容 |
|------|------|
| **用途** | 計算 FarmHash 雜湊值（CityHash 後繼） |
| **技術重點** | FarmHash64/128、跨平台一致 |
| **WASM 模組** | hash-wasm (~200 KB) |
| **執行流程** | 1. 輸入資料 → 2. 計算 → 3. 顯示雜湊 |
| **輸入格式** | 文字、任意檔案 |
| **輸出格式** | 十六進位雜湊值 |
| **效能需求** | 記憶體：低、處理時間：極快 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-046 HMAC 計算

| 欄位 | 內容 |
|------|------|
| **用途** | 計算 HMAC 訊息認證碼 |
| **技術重點** | HMAC-MD5/SHA1/SHA256/SHA512、金鑰訊息認證 |
| **WASM 模組** | crypto-wasm (~500 KB) |
| **執行流程** | 1. 選擇雜湊 → 2. 輸入金鑰 → 3. 輸入訊息 → 4. 計算 |
| **輸入格式** | 金鑰、訊息文字 |
| **輸出格式** | 十六進位 HMAC |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-047 PBKDF2 金鑰衍生

| 欄位 | 內容 |
|------|------|
| **用途** | 從密碼衍生加密金鑰 |
| **技術重點** | PBKDF2、迭代次數、鹽值、輸出長度 |
| **WASM 模組** | crypto-wasm (~500 KB) |
| **執行流程** | 1. 輸入密碼 → 2. 設定鹽值 → 3. 設定迭代 → 4. 衍生金鑰 |
| **輸入格式** | 密碼、鹽值 |
| **輸出格式** | 衍生金鑰（十六進位） |
| **效能需求** | 記憶體：低、處理時間：依迭代次數 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-048 bcrypt 密碼雜湊

| 欄位 | 內容 |
|------|------|
| **用途** | 使用 bcrypt 雜湊密碼 |
| **技術重點** | bcrypt、成本因子、鹽值生成 |
| **WASM 模組** | bcrypt-wasm (~400 KB) |
| **執行流程** | 1. 輸入密碼 → 2. 設定成本 → 3. 雜湊 → 4. 顯示結果 |
| **輸入格式** | 密碼文字 |
| **輸出格式** | bcrypt 雜湊字串 |
| **效能需求** | 記憶體：中等、處理時間：依成本因子 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-049 bcrypt 密碼驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證 bcrypt 雜湊密碼 |
| **技術重點** | bcrypt 驗證、時間恆定比較 |
| **WASM 模組** | bcrypt-wasm (~400 KB) |
| **執行流程** | 1. 輸入密碼 → 2. 輸入雜湊 → 3. 驗證 → 4. 顯示結果 |
| **輸入格式** | 密碼、bcrypt 雜湊 |
| **輸出格式** | 驗證結果 |
| **效能需求** | 記憶體：中等、處理時間：依成本因子 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-050 scrypt 金鑰衍生

| 欄位 | 內容 |
|------|------|
| **用途** | 使用 scrypt 衍生金鑰（記憶體硬） |
| **技術重點** | scrypt、N/r/p 參數、抗 ASIC |
| **WASM 模組** | scrypt-wasm (~500 KB) |
| **執行流程** | 1. 輸入密碼 → 2. 設定參數 → 3. 衍生 → 4. 顯示金鑰 |
| **輸入格式** | 密碼、鹽值 |
| **輸出格式** | 衍生金鑰 |
| **效能需求** | 記憶體：高（依 N）、處理時間：較長 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-051 Argon2 密碼雜湊

| 欄位 | 內容 |
|------|------|
| **用途** | 使用 Argon2 雜湊密碼（推薦標準） |
| **技術重點** | Argon2i/Argon2d/Argon2id、時間/記憶體成本 |
| **WASM 模組** | argon2-wasm (~600 KB) |
| **執行流程** | 1. 輸入密碼 → 2. 選擇變體 → 3. 設定參數 → 4. 雜湊 |
| **輸入格式** | 密碼文字 |
| **輸出格式** | Argon2 雜湊字串 |
| **效能需求** | 記憶體：依設定、處理時間：依設定 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-052 Argon2 密碼驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證 Argon2 雜湊密碼 |
| **技術重點** | Argon2 驗證 |
| **WASM 模組** | argon2-wasm (~600 KB) |
| **執行流程** | 1. 輸入密碼 → 2. 輸入雜湊 → 3. 驗證 |
| **輸入格式** | 密碼、Argon2 雜湊 |
| **輸出格式** | 驗證結果 |
| **效能需求** | 記憶體：依雜湊參數、處理時間：依參數 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-053 HKDF 金鑰衍生

| 欄位 | 內容 |
|------|------|
| **用途** | 使用 HKDF 衍生多個金鑰 |
| **技術重點** | HKDF-Extract/Expand、info 參數、多金鑰衍生 |
| **WASM 模組** | crypto-wasm (~500 KB) |
| **執行流程** | 1. 輸入金鑰材料 → 2. 設定 info → 3. 設定長度 → 4. 衍生 |
| **輸入格式** | 金鑰材料、鹽值、info |
| **輸出格式** | 衍生金鑰 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-054 密碼強度檢測

| 欄位 | 內容 |
|------|------|
| **用途** | 分析密碼強度並給出建議 |
| **技術重點** | zxcvbn 演算法、字典攻擊分析、熵計算 |
| **WASM 模組** | zxcvbn-wasm (~800 KB) |
| **執行流程** | 1. 輸入密碼 → 2. 分析 → 3. 顯示強度與建議 |
| **輸入格式** | 密碼文字 |
| **輸出格式** | 強度分數、破解時間估計、建議 |
| **效能需求** | 記憶體：中等、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-055 密碼生成器

| 欄位 | 內容 |
|------|------|
| **用途** | 生成安全隨機密碼 |
| **技術重點** | CSPRNG、字元集選擇、長度控制 |
| **WASM 模組** | 原生 Web Crypto API |
| **執行流程** | 1. 設定長度 → 2. 選擇字元集 → 3. 生成 → 4. 複製 |
| **輸入格式** | 參數設定 |
| **輸出格式** | 隨機密碼 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-056 通行密語生成器

| 欄位 | 內容 |
|------|------|
| **用途** | 生成易記的通行密語（Passphrase） |
| **技術重點** | Diceware、EFF 詞表、熵計算 |
| **WASM 模組** | 原生 Web Crypto API |
| **執行流程** | 1. 選擇詞數 → 2. 選擇分隔符 → 3. 生成 → 4. 複製 |
| **輸入格式** | 參數設定 |
| **輸出格式** | 隨機通行密語 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-057 PGP 金鑰生成

| 欄位 | 內容 |
|------|------|
| **用途** | 生成 PGP/GPG 金鑰對 |
| **技術重點** | OpenPGP、RSA/ECC 金鑰、使用者 ID、過期時間 |
| **WASM 模組** | openpgp-wasm (~2 MB) |
| **執行流程** | 1. 輸入使用者資訊 → 2. 選擇演算法 → 3. 設定密碼 → 4. 生成 |
| **輸入格式** | 使用者資訊、密碼 |
| **輸出格式** | 公鑰、私鑰（ASCII Armor） |
| **效能需求** | 記憶體：中等、處理時間：數秒 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-058 PGP 加密

| 欄位 | 內容 |
|------|------|
| **用途** | 使用 PGP 公鑰加密訊息/檔案 |
| **技術重點** | OpenPGP 加密、多接收者、ASCII Armor |
| **WASM 模組** | openpgp-wasm (~2 MB) |
| **執行流程** | 1. 上傳公鑰 → 2. 輸入訊息/檔案 → 3. 加密 → 4. 下載 |
| **輸入格式** | 公鑰、訊息/檔案 |
| **輸出格式** | 加密訊息（ASCII Armor 或二進位） |
| **效能需求** | 記憶體：檔案大小 x2、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-059 PGP 解密

| 欄位 | 內容 |
|------|------|
| **用途** | 使用 PGP 私鑰解密訊息/檔案 |
| **技術重點** | OpenPGP 解密、私鑰解鎖 |
| **WASM 模組** | openpgp-wasm (~2 MB) |
| **執行流程** | 1. 上傳私鑰 → 2. 輸入密碼 → 3. 上傳加密內容 → 4. 解密 |
| **輸入格式** | 私鑰、密碼、加密訊息 |
| **輸出格式** | 原始訊息/檔案 |
| **效能需求** | 記憶體：檔案大小 x2、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-060 PGP 簽章

| 欄位 | 內容 |
|------|------|
| **用途** | 使用 PGP 私鑰建立數位簽章 |
| **技術重點** | OpenPGP 簽章、分離式/內嵌式簽章 |
| **WASM 模組** | openpgp-wasm (~2 MB) |
| **執行流程** | 1. 上傳私鑰 → 2. 輸入密碼 → 3. 上傳內容 → 4. 簽章 |
| **輸入格式** | 私鑰、密碼、訊息/檔案 |
| **輸出格式** | 簽章（ASCII Armor） |
| **效能需求** | 記憶體：低、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## 下一個檔案

CRY-061 ~ CRY-080 請參閱 [plan4-4.md](plan4-4.md)
