# Plan 2-3: 音訊處理工具 (AUD-041 ~ AUD-060)

> 共 100 個工具，本檔案包含 AUD-041 ~ AUD-060

---

## AUD-041 音訊波形顯示

| 欄位 | 內容 |
|------|------|
| **用途** | 顯示音訊的波形圖 |
| **技術重點** | 波形繪製、縮放平移、時間軸顯示 |
| **WASM 模組** | 原生 Web Audio API (無需 WASM) |
| **執行流程** | 1. 上傳音訊 → 2. 解析波形 → 3. 繪製顯示 |
| **輸入格式** | MP3, WAV, OGG |
| **輸出格式** | 畫面顯示, PNG (波形圖) |
| **效能需求** | 記憶體：音訊大小、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## AUD-042 頻譜分析

| 欄位 | 內容 |
|------|------|
| **用途** | 顯示音訊的頻譜分析圖 |
| **技術重點** | FFT 分析、頻譜圖繪製、即時/靜態顯示 |
| **WASM 模組** | 原生 Web Audio API (無需 WASM) |
| **執行流程** | 1. 上傳音訊 → 2. FFT 分析 → 3. 繪製頻譜圖 |
| **輸入格式** | MP3, WAV, OGG |
| **輸出格式** | 畫面顯示, PNG (頻譜圖) |
| **效能需求** | 記憶體：音訊大小、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## AUD-043 頻譜圖（Spectrogram）

| 欄位 | 內容 |
|------|------|
| **用途** | 生成時間-頻率頻譜圖 |
| **技術重點** | STFT 分析、色彩映射、解析度設定 |
| **WASM 模組** | 原生 Web Audio API (無需 WASM) |
| **執行流程** | 1. 上傳音訊 → 2. STFT 分析 → 3. 生成頻譜圖 → 4. 匯出 |
| **輸入格式** | MP3, WAV |
| **輸出格式** | PNG (頻譜圖) |
| **效能需求** | 記憶體：音訊大小 x2、處理時間：2-5秒 |
| **進度狀態** | ⬜ 待開發 |

---

## AUD-044 BPM 偵測

| 欄位 | 內容 |
|------|------|
| **用途** | 自動偵測音樂的 BPM（每分鐘拍數） |
| **技術重點** | 節拍偵測演算法、能量分析、峰值偵測 |
| **WASM 模組** | 原生 Web Audio API (無需 WASM) |
| **執行流程** | 1. 上傳音訊 → 2. 分析節拍 → 3. 顯示 BPM |
| **輸入格式** | MP3, WAV, OGG |
| **輸出格式** | BPM 數值顯示 |
| **效能需求** | 記憶體：音訊大小、處理時間：2-5秒 |
| **進度狀態** | ⬜ 待開發 |

---

## AUD-045 音調/音高偵測

| 欄位 | 內容 |
|------|------|
| **用途** | 偵測音訊的主要音調 |
| **技術重點** | 基頻偵測、自相關分析、音符識別 |
| **WASM 模組** | 原生 Web Audio API (無需 WASM) |
| **執行流程** | 1. 上傳音訊 → 2. 分析音高 → 3. 顯示音符 |
| **輸入格式** | MP3, WAV |
| **輸出格式** | 音高/音符顯示 |
| **效能需求** | 記憶體：音訊大小、處理時間：1-3秒 |
| **進度狀態** | ⬜ 待開發 |

---

## AUD-046 音訊資訊顯示

| 欄位 | 內容 |
|------|------|
| **用途** | 顯示音訊檔案詳細資訊 |
| **技術重點** | 格式解析、編碼參數、時長計算 |
| **WASM 模組** | ffmpeg-wasm (~25 MB) |
| **執行流程** | 1. 上傳音訊 → 2. 解析資訊 → 3. 格式化顯示 |
| **輸入格式** | 所有音訊格式 |
| **輸出格式** | 畫面顯示, JSON |
| **效能需求** | 記憶體：極低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## AUD-047 語音轉文字

| 欄位 | 內容 |
|------|------|
| **用途** | 將語音音訊轉換為文字 |
| **技術重點** | Whisper 模型、多語言支援、時間戳記 |
| **WASM 模組** | whisper-wasm (~80 MB) |
| **執行流程** | 1. 上傳音訊 → 2. 語音辨識 → 3. 輸出文字 |
| **輸入格式** | MP3, WAV, OGG |
| **輸出格式** | TXT, SRT (字幕) |
| **效能需求** | 記憶體：高（依模型）、處理時間：音訊長度 x1-2 |
| **進度狀態** | ⬜ 待開發 |

---

## AUD-048 人聲分離

| 欄位 | 內容 |
|------|------|
| **用途** | 將音樂中的人聲與伴奏分離 |
| **技術重點** | 音源分離模型、頻譜遮罩、相位處理 |
| **WASM 模組** | onnx-runtime-wasm + 分離模型 (~100 MB) |
| **執行流程** | 1. 上傳音樂 → 2. AI 分析 → 3. 分離輸出 → 4. 下載 |
| **輸入格式** | MP3, WAV |
| **輸出格式** | WAV (人聲), WAV (伴奏) |
| **效能需求** | 記憶體：高、處理時間：音訊長度 x2-5 |
| **進度狀態** | ⬜ 待開發 |

---

## AUD-049 樂器分離

| 欄位 | 內容 |
|------|------|
| **用途** | 分離音樂中的不同樂器音軌 |
| **技術重點** | 多音源分離、鼓/貝斯/其他分離 |
| **WASM 模組** | onnx-runtime-wasm + 分離模型 (~100 MB) |
| **執行流程** | 1. 上傳音樂 → 2. AI 分析 → 3. 分離各軌 → 4. 打包下載 |
| **輸入格式** | MP3, WAV |
| **輸出格式** | ZIP (多個 WAV 檔) |
| **效能需求** | 記憶體：高、處理時間：音訊長度 x3-10 |
| **進度狀態** | ⬜ 待開發 |

---

## AUD-050 卡拉 OK 製作

| 欄位 | 內容 |
|------|------|
| **用途** | 移除歌曲中的人聲，製作卡拉 OK 版本 |
| **技術重點** | 中央聲道消除、人聲分離、混音調整 |
| **WASM 模組** | onnx-runtime-wasm + 分離模型 |
| **執行流程** | 1. 上傳歌曲 → 2. 移除人聲 → 3. 調整混音 → 4. 下載 |
| **輸入格式** | MP3, WAV |
| **輸出格式** | MP3, WAV |
| **效能需求** | 記憶體：高、處理時間：音訊長度 x2-5 |
| **進度狀態** | ⬜ 待開發 |

---

## AUD-051 鈴聲製作

| 欄位 | 內容 |
|------|------|
| **用途** | 製作手機鈴聲（裁切、轉格式） |
| **技術重點** | 30秒限制、M4R 格式、淡入淡出 |
| **WASM 模組** | ffmpeg-wasm (~25 MB) |
| **執行流程** | 1. 上傳音訊 → 2. 選取區段 → 3. 轉換格式 → 4. 下載 |
| **輸入格式** | MP3, WAV, OGG |
| **輸出格式** | M4R (iPhone), MP3 (Android) |
| **效能需求** | 記憶體：音訊大小、處理時間：1-3秒 |
| **進度狀態** | ⬜ 待開發 |

---

## AUD-052 鬧鐘音效製作

| 欄位 | 內容 |
|------|------|
| **用途** | 製作適合做鬧鐘的音效（漸進式音量） |
| **技術重點** | 音量漸增、循環處理、長度控制 |
| **WASM 模組** | ffmpeg-wasm (~25 MB) |
| **執行流程** | 1. 上傳音訊 → 2. 設定漸進參數 → 3. 處理 → 4. 下載 |
| **輸入格式** | MP3, WAV |
| **輸出格式** | MP3, WAV |
| **效能需求** | 記憶體：音訊大小 x2、處理時間：1-3秒 |
| **進度狀態** | ⬜ 待開發 |

---

## AUD-053 音訊混音

| 欄位 | 內容 |
|------|------|
| **用途** | 將多個音訊混合成一個 |
| **技術重點** | 多軌混音、音量平衡、時間對齊 |
| **WASM 模組** | ffmpeg-wasm (~25 MB) |
| **執行流程** | 1. 上傳多個音訊 → 2. 調整各軌音量 → 3. 混音 → 4. 下載 |
| **輸入格式** | MP3, WAV |
| **輸出格式** | WAV, MP3 |
| **效能需求** | 記憶體：所有音訊總和 x2、處理時間：依長度 |
| **進度狀態** | ⬜ 待開發 |

---

## AUD-054 背景音樂疊加

| 欄位 | 內容 |
|------|------|
| **用途** | 為語音或影片添加背景音樂 |
| **技術重點** | 音量平衡、自動閃避、時間對齊 |
| **WASM 模組** | ffmpeg-wasm (~25 MB) |
| **執行流程** | 1. 上傳主音訊 → 2. 上傳背景音樂 → 3. 調整混合 → 4. 下載 |
| **輸入格式** | MP3, WAV |
| **輸出格式** | MP3, WAV |
| **效能需求** | 記憶體：兩音訊大小 x2、處理時間：依長度 |
| **進度狀態** | ⬜ 待開發 |

---

## AUD-055 音訊循環製作

| 欄位 | 內容 |
|------|------|
| **用途** | 製作無縫循環的音訊片段 |
| **技術重點** | 循環點偵測、交叉淡化、無縫接點 |
| **WASM 模組** | ffmpeg-wasm (~25 MB) |
| **執行流程** | 1. 上傳音訊 → 2. 設定循環點 → 3. 處理 → 4. 下載 |
| **輸入格式** | MP3, WAV |
| **輸出格式** | WAV, MP3 |
| **效能需求** | 記憶體：音訊大小 x2、處理時間：1-3秒 |
| **進度狀態** | ⬜ 待開發 |

---

## AUD-056 錄音機

| 欄位 | 內容 |
|------|------|
| **用途** | 在瀏覽器中錄製音訊 |
| **技術重點** | MediaRecorder API、即時監聽、格式選擇 |
| **WASM 模組** | 原生 MediaRecorder API (無需 WASM) |
| **執行流程** | 1. 開始錄音 → 2. 即時監聽 → 3. 停止 → 4. 下載 |
| **輸入格式** | 麥克風輸入 |
| **輸出格式** | WebM, WAV, MP3 |
| **效能需求** | 記憶體：依錄音時長、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## AUD-057 節拍器

| 欄位 | 內容 |
|------|------|
| **用途** | 產生節拍器音效（練習用） |
| **技術重點** | 精確計時、BPM 調整、重音設定 |
| **WASM 模組** | 原生 Web Audio API (無需 WASM) |
| **執行流程** | 1. 設定 BPM → 2. 設定拍號 → 3. 播放/匯出 |
| **輸入格式** | 無 |
| **輸出格式** | 即時播放, WAV |
| **效能需求** | 記憶體：極低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## AUD-058 音調產生器

| 欄位 | 內容 |
|------|------|
| **用途** | 產生特定頻率的音調（正弦波、方波等） |
| **技術重點** | 振盪器類型、頻率控制、波形選擇 |
| **WASM 模組** | 原生 Web Audio API (無需 WASM) |
| **執行流程** | 1. 選擇波形 → 2. 設定頻率 → 3. 播放/匯出 |
| **輸入格式** | 無 |
| **輸出格式** | 即時播放, WAV |
| **效能需求** | 記憶體：極低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## AUD-059 白噪音產生器

| 欄位 | 內容 |
|------|------|
| **用途** | 產生白噪音、粉紅噪音、棕噪音 |
| **技術重點** | 噪音類型、音量控制、時長設定 |
| **WASM 模組** | 原生 Web Audio API (無需 WASM) |
| **執行流程** | 1. 選擇噪音類型 → 2. 設定參數 → 3. 播放/匯出 |
| **輸入格式** | 無 |
| **輸出格式** | 即時播放, WAV |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## AUD-060 音效庫播放器

| 欄位 | 內容 |
|------|------|
| **用途** | 播放常用音效（提示音、警告音等） |
| **技術重點** | 音效分類、預覽播放、下載功能 |
| **WASM 模組** | 原生 Web Audio API (無需 WASM) |
| **執行流程** | 1. 瀏覽音效庫 → 2. 預覽試聽 → 3. 下載選擇的音效 |
| **輸入格式** | 無 |
| **輸出格式** | MP3, WAV |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## 下一個檔案

AUD-061 ~ AUD-080 請參閱 [plan2-4.md](plan2-4.md)
