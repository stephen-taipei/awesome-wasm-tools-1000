# Plan 6-3: PDF 文件工具 (PDF-041 ~ PDF-060)

> 共 120 個工具，本檔案包含 PDF-041 ~ PDF-060

---

## PDF-041 PDF 小冊子排版

| 欄位 | 內容 |
|------|------|
| **用途** | 將 PDF 排版為可摺疊小冊子 |
| **技術重點** | 頁面重排、騎馬釘、膠裝 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇裝訂方式 → 3. 排版 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（小冊子排版） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-042 PDF 雙面列印排版

| 欄位 | 內容 |
|------|------|
| **用途** | 為雙面列印重新排列頁面 |
| **技術重點** | 長邊/短邊翻轉、頁面順序 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇翻轉方式 → 3. 排版 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（雙面排版） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-043 PDF 表單建立

| 欄位 | 內容 |
|------|------|
| **用途** | 在 PDF 中建立可填寫表單 |
| **技術重點** | 文字欄位、核取方塊、下拉選單、按鈕 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 添加表單欄位 → 3. 設定屬性 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（含表單） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依複雜度 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-044 PDF 表單填寫

| 欄位 | 內容 |
|------|------|
| **用途** | 填寫 PDF 表單 |
| **技術重點** | 欄位識別、資料輸入、驗證 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF 表單 → 2. 填寫欄位 → 3. 儲存 → 4. 下載 |
| **輸入格式** | PDF（含表單） |
| **輸出格式** | PDF（已填寫） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-045 PDF 表單資料提取

| 欄位 | 內容 |
|------|------|
| **用途** | 從 PDF 表單提取填寫資料 |
| **技術重點** | 欄位值提取、FDF/XFDF 匯出 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳已填寫 PDF → 2. 提取資料 → 3. 匯出 JSON/CSV |
| **輸入格式** | PDF（已填寫） |
| **輸出格式** | JSON/CSV/FDF |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-046 PDF 表單扁平化

| 欄位 | 內容 |
|------|------|
| **用途** | 將表單欄位轉為靜態內容 |
| **技術重點** | 欄位扁平化、外觀保留 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF 表單 → 2. 扁平化 → 3. 下載 |
| **輸入格式** | PDF（含表單） |
| **輸出格式** | PDF（靜態） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-047 PDF 批次表單填寫

| 欄位 | 內容 |
|------|------|
| **用途** | 使用資料批次填寫多份表單 |
| **技術重點** | 資料匯入、批次處理、合併輸出 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳表單 → 2. 上傳資料 → 3. 批次填寫 → 4. 打包下載 |
| **輸入格式** | PDF 表單、CSV 資料 |
| **輸出格式** | 多個 PDF |
| **效能需求** | 記憶體：依資料量、處理時間：依數量 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-048 PDF 註解添加

| 欄位 | 內容 |
|------|------|
| **用途** | 在 PDF 上添加註解 |
| **技術重點** | 高亮、底線、刪除線、便條、繪圖 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇工具 → 3. 添加註解 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（含註解） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-049 PDF 註解提取

| 欄位 | 內容 |
|------|------|
| **用途** | 從 PDF 提取所有註解 |
| **技術重點** | 註解解析、內容提取、位置記錄 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 提取註解 → 3. 匯出列表 |
| **輸入格式** | PDF（含註解） |
| **輸出格式** | JSON/TXT |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-050 PDF 註解移除

| 欄位 | 內容 |
|------|------|
| **用途** | 移除 PDF 中的註解 |
| **技術重點** | 選擇性移除、全部移除 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇註解 → 3. 移除 → 4. 下載 |
| **輸入格式** | PDF（含註解） |
| **輸出格式** | PDF（註解已移除） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-051 PDF 圖片提取

| 欄位 | 內容 |
|------|------|
| **用途** | 從 PDF 提取所有圖片 |
| **技術重點** | 圖片識別、格式保留、批次提取 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 掃描圖片 → 3. 選擇提取 → 4. 打包下載 |
| **輸入格式** | PDF |
| **輸出格式** | PNG/JPG 圖片 |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依圖片數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-052 PDF 字型提取

| 欄位 | 內容 |
|------|------|
| **用途** | 從 PDF 提取嵌入字型 |
| **技術重點** | 字型解析、格式轉換、子集還原 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 列出字型 → 3. 選擇提取 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | TTF/OTF |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-053 PDF 字型嵌入

| 欄位 | 內容 |
|------|------|
| **用途** | 將外部字型嵌入 PDF |
| **技術重點** | 字型嵌入、子集化、相容性 |
| **WASM 模組** | pdf-lib-wasm + fontkit-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 上傳字型 → 3. 嵌入 → 4. 下載 |
| **輸入格式** | PDF、TTF/OTF |
| **輸出格式** | PDF（字型已嵌入） |
| **效能需求** | 記憶體：依字型大小、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-054 PDF 字型替換

| 欄位 | 內容 |
|------|------|
| **用途** | 替換 PDF 中的字型 |
| **技術重點** | 字型對應、重新渲染 |
| **WASM 模組** | pdf-lib-wasm + fontkit-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇原字型 → 3. 選擇新字型 → 4. 替換 |
| **輸入格式** | PDF、新字型 |
| **輸出格式** | PDF（字型已替換） |
| **效能需求** | 記憶體：依大小、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-055 PDF 圖章添加

| 欄位 | 內容 |
|------|------|
| **用途** | 在 PDF 上添加圖章 |
| **技術重點** | 預設圖章、自訂圖章、位置大小 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇/上傳圖章 → 3. 放置 → 4. 下載 |
| **輸入格式** | PDF、圖章圖片 |
| **輸出格式** | PDF（含圖章） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-056 PDF 手寫簽名

| 欄位 | 內容 |
|------|------|
| **用途** | 在 PDF 上添加手寫簽名 |
| **技術重點** | 畫布簽名、圖片簽名、觸控支援 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 繪製/上傳簽名 → 3. 放置 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（含簽名） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-057 PDF 文字編輯

| 欄位 | 內容 |
|------|------|
| **用途** | 編輯 PDF 中的文字內容 |
| **技術重點** | 文字定位、內容修改、格式保留 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇文字 → 3. 編輯 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（已編輯） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-058 PDF 文字添加

| 欄位 | 內容 |
|------|------|
| **用途** | 在 PDF 上添加新文字 |
| **技術重點** | 字型選擇、大小顏色、定位 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 輸入文字 → 3. 設定樣式 → 4. 放置 → 5. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（含新文字） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-059 PDF 圖片添加

| 欄位 | 內容 |
|------|------|
| **用途** | 在 PDF 中插入圖片 |
| **技術重點** | 圖片格式、大小調整、位置 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 上傳圖片 → 3. 調整放置 → 4. 下載 |
| **輸入格式** | PDF、PNG/JPG |
| **輸出格式** | PDF（含圖片） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-060 PDF 頁面背景

| 欄位 | 內容 |
|------|------|
| **用途** | 設定 PDF 頁面背景 |
| **技術重點** | 顏色/圖片背景、透明度、單頁/全部 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇背景 → 3. 設定選項 → 4. 套用 → 5. 下載 |
| **輸入格式** | PDF、背景圖片（選用） |
| **輸出格式** | PDF（含背景） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## 下一個檔案

PDF-061 ~ PDF-080 請參閱 [plan6-4.md](plan6-4.md)
