# Plan 6-5: PDF 文件工具 (PDF-081 ~ PDF-100)

> 共 120 個工具，本檔案包含 PDF-081 ~ PDF-100

---

## PDF-081 PDF 轉 Markdown

| 欄位 | 內容 |
|------|------|
| **用途** | 將 PDF 轉換為 Markdown 格式 |
| **技術重點** | 標題識別、列表轉換、程式碼區塊 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 轉換 → 3. 預覽 → 4. 下載 MD |
| **輸入格式** | PDF |
| **輸出格式** | Markdown |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-082 PDF 轉 LaTeX

| 欄位 | 內容 |
|------|------|
| **用途** | 將 PDF 轉換為 LaTeX 格式 |
| **技術重點** | 公式識別、格式轉換、結構保留 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 轉換 → 3. 預覽 → 4. 下載 TEX |
| **輸入格式** | PDF |
| **輸出格式** | LaTeX (.tex) |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-083 LaTeX 轉 PDF

| 欄位 | 內容 |
|------|------|
| **用途** | 將 LaTeX 文件編譯為 PDF |
| **技術重點** | LaTeX 編譯、字型嵌入、數學排版 |
| **WASM 模組** | texlive-wasm (~50 MB) |
| **執行流程** | 1. 輸入 LaTeX → 2. 編譯 → 3. 預覽 → 4. 下載 PDF |
| **輸入格式** | LaTeX (.tex) |
| **輸出格式** | PDF |
| **效能需求** | 記憶體：極高、處理時間：依複雜度 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-084 PDF 轉 SVG

| 欄位 | 內容 |
|------|------|
| **用途** | 將 PDF 頁面轉換為 SVG 向量圖 |
| **技術重點** | 向量提取、路徑轉換、文字保留 |
| **WASM 模組** | pdf2svg-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇頁面 → 3. 轉換 → 4. 下載 SVG |
| **輸入格式** | PDF |
| **輸出格式** | SVG |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-085 SVG 轉 PDF

| 欄位 | 內容 |
|------|------|
| **用途** | 將 SVG 向量圖轉換為 PDF |
| **技術重點** | 向量轉換、頁面設定、多檔合併 |
| **WASM 模組** | svg2pdf-wasm (~2 MB) |
| **執行流程** | 1. 上傳 SVG → 2. 設定頁面 → 3. 轉換 → 4. 下載 PDF |
| **輸入格式** | SVG |
| **輸出格式** | PDF |
| **效能需求** | 記憶體：中等、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-086 PDF 轉 XPS

| 欄位 | 內容 |
|------|------|
| **用途** | 將 PDF 轉換為 XPS 格式 |
| **技術重點** | XPS 規範、字型處理、向量轉換 |
| **WASM 模組** | mupdf-wasm (~8 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 轉換 → 3. 下載 XPS |
| **輸入格式** | PDF |
| **輸出格式** | XPS |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-087 XPS 轉 PDF

| 欄位 | 內容 |
|------|------|
| **用途** | 將 XPS 轉換為 PDF 格式 |
| **技術重點** | XPS 解析、PDF 生成 |
| **WASM 模組** | mupdf-wasm (~8 MB) |
| **執行流程** | 1. 上傳 XPS → 2. 轉換 → 3. 下載 PDF |
| **輸入格式** | XPS |
| **輸出格式** | PDF |
| **效能需求** | 記憶體：檔案大小 x2、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-088 PDF 轉 TIFF

| 欄位 | 內容 |
|------|------|
| **用途** | 將 PDF 轉換為多頁 TIFF 圖片 |
| **技術重點** | 高解析度渲染、多頁 TIFF、壓縮選項 |
| **WASM 模組** | pdfjs-wasm + tiff-wasm (~5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 設定解析度 → 3. 轉換 → 4. 下載 TIFF |
| **輸入格式** | PDF |
| **輸出格式** | TIFF |
| **效能需求** | 記憶體：高、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-089 TIFF 轉 PDF

| 欄位 | 內容 |
|------|------|
| **用途** | 將 TIFF 圖片轉換為 PDF |
| **技術重點** | 多頁 TIFF 支援、頁面設定 |
| **WASM 模組** | tiff-wasm + pdf-lib-wasm (~4 MB) |
| **執行流程** | 1. 上傳 TIFF → 2. 設定頁面 → 3. 轉換 → 4. 下載 PDF |
| **輸入格式** | TIFF |
| **輸出格式** | PDF |
| **效能需求** | 記憶體：依圖片大小、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-090 PDF 頁面插入

| 欄位 | 內容 |
|------|------|
| **用途** | 在 PDF 中插入新頁面 |
| **技術重點** | 頁面位置、空白頁/圖片頁、頁面設定 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇位置 → 3. 插入頁面 → 4. 下載 |
| **輸入格式** | PDF、頁面內容 |
| **輸出格式** | PDF（已插入） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-091 PDF 頁面替換

| 欄位 | 內容 |
|------|------|
| **用途** | 替換 PDF 中的指定頁面 |
| **技術重點** | 頁面選擇、內容替換、格式保留 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇頁面 → 3. 上傳替換內容 → 4. 下載 |
| **輸入格式** | PDF、替換頁面 |
| **輸出格式** | PDF（已替換） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-092 PDF 頁面複製

| 欄位 | 內容 |
|------|------|
| **用途** | 複製 PDF 中的頁面 |
| **技術重點** | 頁面複製、位置指定、多次複製 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇頁面 → 3. 設定複製位置/次數 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（含複製頁面） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-093 PDF 批次處理

| 欄位 | 內容 |
|------|------|
| **用途** | 對多個 PDF 執行相同操作 |
| **技術重點** | 批次上傳、並行處理、統一設定 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳多個 PDF → 2. 選擇操作 → 3. 批次執行 → 4. 打包下載 |
| **輸入格式** | 多個 PDF |
| **輸出格式** | 多個處理後 PDF |
| **效能需求** | 記憶體：依總大小、處理時間：依數量 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-094 PDF 批次重命名

| 欄位 | 內容 |
|------|------|
| **用途** | 批次重命名 PDF 檔案 |
| **技術重點** | 命名規則、序號、元資料提取 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 上傳多個 PDF → 2. 設定命名規則 → 3. 預覽 → 4. 打包下載 |
| **輸入格式** | 多個 PDF |
| **輸出格式** | 重命名後的 PDF |
| **效能需求** | 記憶體：低、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-095 PDF 批次轉換

| 欄位 | 內容 |
|------|------|
| **用途** | 批次將 PDF 轉換為其他格式 |
| **技術重點** | 格式選擇、並行轉換、統一設定 |
| **WASM 模組** | 依目標格式載入 |
| **執行流程** | 1. 上傳多個 PDF → 2. 選擇目標格式 → 3. 轉換 → 4. 打包下載 |
| **輸入格式** | 多個 PDF |
| **輸出格式** | 目標格式檔案 |
| **效能需求** | 記憶體：依總大小、處理時間：依數量 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-096 PDF 預覽器

| 欄位 | 內容 |
|------|------|
| **用途** | 在瀏覽器中預覽 PDF |
| **技術重點** | 縮放、翻頁、全螢幕、書籤導航 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 渲染預覽 → 3. 導航檢視 |
| **輸入格式** | PDF |
| **輸出格式** | 畫面顯示 |
| **效能需求** | 記憶體：依頁數、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-097 PDF 縮圖生成

| 欄位 | 內容 |
|------|------|
| **用途** | 為 PDF 各頁生成縮圖 |
| **技術重點** | 低解析度渲染、批次生成、格式選擇 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 設定縮圖大小 → 3. 生成 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PNG/JPG 縮圖 |
| **效能需求** | 記憶體：中等、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-098 PDF 封面提取

| 欄位 | 內容 |
|------|------|
| **用途** | 提取 PDF 第一頁作為封面 |
| **技術重點** | 高品質渲染、多種解析度、格式選擇 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 設定選項 → 3. 提取 → 4. 下載圖片 |
| **輸入格式** | PDF |
| **輸出格式** | PNG/JPG |
| **效能需求** | 記憶體：中等、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-099 PDF 頁面計數

| 欄位 | 內容 |
|------|------|
| **用途** | 快速獲取 PDF 頁數 |
| **技術重點** | 元資料讀取、批次計數 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 讀取頁數 → 3. 顯示結果 |
| **輸入格式** | PDF（單個或多個） |
| **輸出格式** | 頁數統計 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-100 PDF 檔案大小檢查

| 欄位 | 內容 |
|------|------|
| **用途** | 分析 PDF 檔案大小組成 |
| **技術重點** | 物件分析、圖片/字型大小、最佳化建議 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 分析組成 → 3. 顯示報告 |
| **輸入格式** | PDF |
| **輸出格式** | 大小分析報告 |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## 下一個檔案

PDF-101 ~ PDF-120 請參閱 [plan6-6.md](plan6-6.md)
