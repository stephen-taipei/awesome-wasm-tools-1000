# Plan 7-4: 編碼轉換工具 (ENC-061 ~ ENC-080)

> 共 100 個工具，本檔案包含 ENC-061 ~ ENC-080

---

## ENC-061 時間戳轉換

| 欄位 | 內容 |
|------|------|
| **用途** | Unix 時間戳與日期時間互轉 |
| **技術重點** | 秒/毫秒、時區處理 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入時間戳或日期 → 2. 轉換 → 3. 顯示各格式 |
| **輸入格式** | 時間戳、日期時間 |
| **輸出格式** | 各種時間格式 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-062 日期格式轉換

| 欄位 | 內容 |
|------|------|
| **用途** | 轉換日期顯示格式 |
| **技術重點** | ISO 8601、RFC 2822、自訂格式 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入日期 → 2. 選擇格式 → 3. 轉換 |
| **輸入格式** | 日期 |
| **輸出格式** | 格式化日期 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-063 時區轉換

| 欄位 | 內容 |
|------|------|
| **用途** | 在不同時區間轉換時間 |
| **技術重點** | IANA 時區、日光節約、UTC 偏移 |
| **WASM 模組** | 原生 JS (Intl API) |
| **執行流程** | 1. 輸入時間 → 2. 選擇來源/目標時區 → 3. 轉換 |
| **輸入格式** | 時間、時區 |
| **輸出格式** | 轉換後時間 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-064 農曆轉換

| 欄位 | 內容 |
|------|------|
| **用途** | 國曆與農曆互轉 |
| **技術重點** | 農曆算法、閏月處理、節氣 |
| **WASM 模組** | lunar-wasm (~200 KB) |
| **執行流程** | 1. 輸入日期 → 2. 轉換 → 3. 顯示農曆/節氣 |
| **輸入格式** | 國曆或農曆日期 |
| **輸出格式** | 農曆/國曆 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-065 世界時間顯示

| 欄位 | 內容 |
|------|------|
| **用途** | 同時顯示多個時區時間 |
| **技術重點** | 即時更新、城市搜尋 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 選擇城市/時區 → 2. 即時顯示時間 |
| **輸入格式** | 時區選擇 |
| **輸出格式** | 多時區時間 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-066 Cron 表達式解析

| 欄位 | 內容 |
|------|------|
| **用途** | 解析 Cron 表達式 |
| **技術重點** | 5/6 欄位格式、下次執行時間 |
| **WASM 模組** | cronstrue-wasm (~50 KB) |
| **執行流程** | 1. 輸入 Cron → 2. 解析 → 3. 顯示說明/下次執行 |
| **輸入格式** | Cron 表達式 |
| **輸出格式** | 人類可讀說明 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-067 Cron 表達式生成

| 欄位 | 內容 |
|------|------|
| **用途** | 視覺化生成 Cron 表達式 |
| **技術重點** | 時間選擇器、預覽驗證 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 選擇時間規則 → 2. 生成 Cron → 3. 複製 |
| **輸入格式** | 視覺化選擇 |
| **輸出格式** | Cron 表達式 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-068 UUID 生成

| 欄位 | 內容 |
|------|------|
| **用途** | 生成 UUID |
| **技術重點** | v1/v4/v5/v7、批次生成 |
| **WASM 模組** | 原生 JS (crypto) |
| **執行流程** | 1. 選擇版本 → 2. 生成 → 3. 複製 |
| **輸入格式** | 版本選擇 |
| **輸出格式** | UUID |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-069 UUID 解析

| 欄位 | 內容 |
|------|------|
| **用途** | 解析 UUID 資訊 |
| **技術重點** | 版本識別、時間戳提取（v1/v7） |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入 UUID → 2. 解析 → 3. 顯示詳細資訊 |
| **輸入格式** | UUID |
| **輸出格式** | 版本、時間戳等 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-070 ULID 生成

| 欄位 | 內容 |
|------|------|
| **用途** | 生成 ULID |
| **技術重點** | 時間排序、URL 安全 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 點擊生成 → 2. 複製 |
| **輸入格式** | 無 |
| **輸出格式** | ULID |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-071 ULID 解析

| 欄位 | 內容 |
|------|------|
| **用途** | 解析 ULID 時間戳 |
| **技術重點** | 時間提取、驗證 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入 ULID → 2. 解析 → 3. 顯示時間 |
| **輸入格式** | ULID |
| **輸出格式** | 時間戳 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-072 短網址生成

| 欄位 | 內容 |
|------|------|
| **用途** | 生成短識別碼 |
| **技術重點** | NanoID、自訂長度/字母表 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 設定長度 → 2. 生成 → 3. 複製 |
| **輸入格式** | 設定參數 |
| **輸出格式** | 短識別碼 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-073 Snowflake ID 生成

| 欄位 | 內容 |
|------|------|
| **用途** | 生成 Snowflake 分散式 ID |
| **技術重點** | 時間戳、機器 ID、序號 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 設定機器 ID → 2. 生成 → 3. 複製 |
| **輸入格式** | 機器 ID |
| **輸出格式** | Snowflake ID |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-074 Snowflake ID 解析

| 欄位 | 內容 |
|------|------|
| **用途** | 解析 Snowflake ID 組成 |
| **技術重點** | 時間戳、機器 ID、序號提取 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入 ID → 2. 解析 → 3. 顯示各欄位 |
| **輸入格式** | Snowflake ID |
| **輸出格式** | 組成欄位 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-075 JWT 編碼

| 欄位 | 內容 |
|------|------|
| **用途** | 建立 JWT Token |
| **技術重點** | Header/Payload 編輯、簽章 |
| **WASM 模組** | jose-wasm (~500 KB) |
| **執行流程** | 1. 編輯 Header/Payload → 2. 輸入密鑰 → 3. 生成 JWT |
| **輸入格式** | JSON、密鑰 |
| **輸出格式** | JWT Token |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-076 JWT 解碼

| 欄位 | 內容 |
|------|------|
| **用途** | 解碼 JWT Token |
| **技術重點** | Header/Payload 解析、過期檢查 |
| **WASM 模組** | 原生 JS |
| **執行流程** | 1. 輸入 JWT → 2. 解碼 → 3. 顯示內容 |
| **輸入格式** | JWT Token |
| **輸出格式** | Header、Payload、簽章 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-077 JWT 驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證 JWT 簽章 |
| **技術重點** | 簽章驗證、過期檢查 |
| **WASM 模組** | jose-wasm (~500 KB) |
| **執行流程** | 1. 輸入 JWT → 2. 輸入密鑰 → 3. 驗證 |
| **輸入格式** | JWT、密鑰 |
| **輸出格式** | 驗證結果 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-078 TOTP 生成

| 欄位 | 內容 |
|------|------|
| **用途** | 生成 TOTP 一次性密碼 |
| **技術重點** | RFC 6238、時間同步、即時更新 |
| **WASM 模組** | 原生 JS (crypto) |
| **執行流程** | 1. 輸入密鑰 → 2. 即時顯示 TOTP |
| **輸入格式** | Base32 密鑰 |
| **輸出格式** | 6 位數 TOTP |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-079 TOTP 驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證 TOTP 一次性密碼 |
| **技術重點** | 時間視窗、容錯驗證 |
| **WASM 模組** | 原生 JS (crypto) |
| **執行流程** | 1. 輸入密鑰 → 2. 輸入 TOTP → 3. 驗證 |
| **輸入格式** | 密鑰、TOTP |
| **輸出格式** | 驗證結果 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## ENC-080 OTP 密鑰生成

| 欄位 | 內容 |
|------|------|
| **用途** | 生成 OTP 設定 URI 與 QR Code |
| **技術重點** | otpauth URI、QR Code 生成 |
| **WASM 模組** | 原生 JS + qrcode-wasm |
| **執行流程** | 1. 設定帳號/發行者 → 2. 生成密鑰 → 3. 顯示 QR Code |
| **輸入格式** | 帳號資訊 |
| **輸出格式** | 密鑰、QR Code |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## 下一個檔案

ENC-081 ~ ENC-100 請參閱 [plan7-5.md](plan7-5.md)
