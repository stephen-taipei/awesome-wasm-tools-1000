# Plan 4-7: 加密解密工具 (CRY-121 ~ CRY-130)

> 共 130 個工具，本檔案包含 CRY-121 ~ CRY-130（最終檔案）

---

## CRY-121 PEM 格式化工具

| 欄位 | 內容 |
|------|------|
| **用途** | 格式化、驗證 PEM 檔案 |
| **技術重點** | PEM 標頭/尾檢查、Base64 格式化、多區塊處理 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 上傳/貼上 PEM → 2. 格式化 → 3. 驗證 → 4. 下載 |
| **輸入格式** | PEM 檔案/文字 |
| **輸出格式** | 格式化 PEM |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-122 金鑰強度分析

| 欄位 | 內容 |
|------|------|
| **用途** | 分析加密金鑰的安全強度 |
| **技術重點** | 金鑰長度評估、演算法安全性、建議等級 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 上傳金鑰 → 2. 分析 → 3. 顯示評估報告 |
| **輸入格式** | 各類金鑰格式 |
| **輸出格式** | 安全性報告 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-123 Hex/Binary/Dec 轉換器

| 欄位 | 內容 |
|------|------|
| **用途** | 十六進位/二進位/十進位互轉 |
| **技術重點** | 大數支援、任意進位、雙向轉換 |
| **WASM 模組** | 原生 JS (BigInt) |
| **執行流程** | 1. 輸入數值 → 2. 選擇來源進位 → 3. 轉換 → 4. 顯示結果 |
| **輸入格式** | 數值字串 |
| **輸出格式** | 多進位顯示 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-124 位元運算器

| 欄位 | 內容 |
|------|------|
| **用途** | 執行位元運算（AND/OR/XOR/NOT/Shift） |
| **技術重點** | 位元操作、視覺化顯示、大數支援 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入運算元 → 2. 選擇運算 → 3. 執行 → 4. 顯示結果 |
| **輸入格式** | 數值（任意進位） |
| **輸出格式** | 運算結果 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-125 OpenSSL 指令生成器

| 欄位 | 內容 |
|------|------|
| **用途** | 生成常用 OpenSSL 命令列指令 |
| **技術重點** | 指令模板、參數說明、複製貼上 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 選擇操作類型 → 2. 填入參數 → 3. 生成指令 → 4. 複製 |
| **輸入格式** | 參數選擇 |
| **輸出格式** | OpenSSL 指令 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-126 安全資料清除

| 欄位 | 內容 |
|------|------|
| **用途** | 安全清除記憶體中的敏感資料 |
| **技術重點** | 多次覆寫、零化、防止最佳化 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入資料 → 2. 處理 → 3. 安全清除 → 4. 確認 |
| **輸入格式** | 敏感資料 |
| **輸出格式** | 清除確認 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-127 憑證過期監控

| 欄位 | 內容 |
|------|------|
| **用途** | 檢查憑證過期時間並提醒 |
| **技術重點** | 過期日計算、本地儲存、通知 |
| **WASM 模組** | pkijs-wasm (~1 MB) |
| **執行流程** | 1. 上傳憑證 → 2. 解析到期日 → 3. 設定提醒 → 4. 本地追蹤 |
| **輸入格式** | X.509 憑證 |
| **輸出格式** | 到期資訊、提醒設定 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-128 公開金鑰基礎設施視覺化

| 欄位 | 內容 |
|------|------|
| **用途** | 視覺化顯示 PKI 憑證關係 |
| **技術重點** | 憑證鏈圖表、信任路徑、互動式 |
| **WASM 模組** | pkijs-wasm (~1 MB) |
| **執行流程** | 1. 上傳憑證鏈 → 2. 解析關係 → 3. 顯示圖表 |
| **輸入格式** | 多個 X.509 憑證 |
| **輸出格式** | 互動式圖表 |
| **效能需求** | 記憶體：中等、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-129 密碼學術語詞典

| 欄位 | 內容 |
|------|------|
| **用途** | 密碼學術語查詢與解釋 |
| **技術重點** | 術語資料庫、搜尋功能、關聯詞 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入術語 → 2. 搜尋 → 3. 顯示解釋與相關詞 |
| **輸入格式** | 搜尋關鍵字 |
| **輸出格式** | 術語解釋 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-130 加密解密工作流

| 欄位 | 內容 |
|------|------|
| **用途** | 建立自訂加密處理流程 |
| **技術重點** | 工作流引擎、步驟串接、自動化處理 |
| **WASM 模組** | 依工作流步驟載入 |
| **執行流程** | 1. 建立工作流 → 2. 上傳資料 → 3. 執行流程 → 4. 下載結果 |
| **輸入格式** | 任意資料 |
| **輸出格式** | 依工作流設定 |
| **效能需求** | 記憶體：依步驟、處理時間：依步驟 |
| **進度狀態** | ⬜ 待開發 |

---

## 加密解密工具總覽

| 分類 | 工具編號 | 數量 | 檔案 |
|------|----------|------|------|
| 對稱加密 | CRY-001~028 | 28 | plan4.md, plan4-2.md |
| 雜湊函數 | CRY-029~045 | 17 | plan4-2.md, plan4-3.md |
| 金鑰衍生 | CRY-046~053 | 8 | plan4-3.md |
| 密碼工具 | CRY-054~056 | 3 | plan4-3.md |
| PGP | CRY-057~061 | 5 | plan4-3.md, plan4-4.md |
| SSH | CRY-062~064 | 3 | plan4-4.md |
| X.509/PKI | CRY-065~071 | 7 | plan4-4.md |
| JWT/JWK/JWE | CRY-072~078 | 7 | plan4-4.md |
| OTP/2FA | CRY-079~081 | 3 | plan4-4.md, plan4-5.md |
| 區塊鏈 | CRY-082~088 | 7 | plan4-5.md |
| 秘密分享 | CRY-089~090 | 2 | plan4-5.md |
| 檔案加密 | CRY-091~095 | 5 | plan4-5.md |
| ID 生成 | CRY-096~100 | 5 | plan4-5.md |
| 金鑰交換 | CRY-101~102 | 2 | plan4-6.md |
| 古典密碼 | CRY-103~112 | 10 | plan4-6.md |
| 分析工具 | CRY-113~120 | 8 | plan4-6.md |
| 輔助工具 | CRY-121~130 | 10 | plan4-7.md |

---

## 下一個類別

文字分析工具 (TXT-001 ~ TXT-150) 請參閱 [plan5.md](plan5.md)
