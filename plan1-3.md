# Plan 1-3: 圖片處理工具 (IMG-041 ~ IMG-060)

> 共 150 個工具，本檔案包含 IMG-041 ~ IMG-060

---

## IMG-041 查看 EXIF 資訊

| 欄位 | 內容 |
|------|------|
| **用途** | 查看圖片的 EXIF 元資料（拍攝參數、GPS、設備資訊） |
| **技術重點** | EXIF 解析、GPS 座標轉換、日期時間格式化 |
| **WASM 模組** | exif-wasm (~500 KB) |
| **執行流程** | 1. 上傳圖片 → 2. 解析 EXIF → 3. 格式化顯示 |
| **輸入格式** | JPG, TIFF, HEIC |
| **輸出格式** | 畫面顯示, JSON 匯出 |
| **效能需求** | 記憶體：極低、處理時間：即時 |
| **進度狀態** | ✅ 已完成 |

---

## IMG-042 編輯 EXIF 資訊

| 欄位 | 內容 |
|------|------|
| **用途** | 編輯或修改圖片的 EXIF 資訊 |
| **技術重點** | EXIF 讀寫、日期修改、GPS 編輯 |
| **WASM 模組** | exif-wasm (~500 KB) |
| **執行流程** | 1. 上傳圖片 → 2. 顯示 EXIF → 3. 編輯欄位 → 4. 儲存 |
| **輸入格式** | JPG, TIFF |
| **輸出格式** | JPG, TIFF |
| **效能需求** | 記憶體：圖片大小、處理時間：<1秒 |
| **進度狀態** | ✅ 已完成 |

---

## IMG-043 清除 EXIF 資訊

| 欄位 | 內容 |
|------|------|
| **用途** | 移除圖片所有 EXIF 資訊以保護隱私 |
| **技術重點** | EXIF 剝離、保留色彩配置檔選項 |
| **WASM 模組** | exif-wasm (~500 KB) |
| **執行流程** | 1. 上傳圖片 → 2. 移除 EXIF → 3. 輸出乾淨圖片 |
| **輸入格式** | JPG, TIFF, PNG |
| **輸出格式** | JPG, PNG |
| **效能需求** | 記憶體：圖片大小、處理時間：即時 |
| **進度狀態** | ✅ 已完成 |

---

## IMG-044 批量清除 EXIF

| 欄位 | 內容 |
|------|------|
| **用途** | 批量移除多張圖片的 EXIF 資訊 |
| **技術重點** | 並行處理、ZIP 打包下載 |
| **WASM 模組** | exif-wasm (~500 KB) |
| **執行流程** | 1. 上傳多圖 → 2. 批量移除 → 3. 打包下載 |
| **輸入格式** | JPG, PNG |
| **輸出格式** | ZIP |
| **效能需求** | 記憶體：依批量大小、處理時間：並行加速 |
| **進度狀態** | ✅ 已完成 |

---

## IMG-045 複製 EXIF 資訊

| 欄位 | 內容 |
|------|------|
| **用途** | 將一張圖片的 EXIF 複製到另一張圖片 |
| **技術重點** | EXIF 提取與注入、選擇性複製 |
| **WASM 模組** | exif-wasm (~500 KB) |
| **執行流程** | 1. 上傳來源圖 → 2. 上傳目標圖 → 3. 複製 EXIF → 4. 輸出 |
| **輸入格式** | JPG, TIFF |
| **輸出格式** | JPG |
| **效能需求** | 記憶體：兩圖大小、處理時間：<1秒 |
| **進度狀態** | ✅ 已完成 |

---

## IMG-046 智慧去背

| 欄位 | 內容 |
|------|------|
| **用途** | AI 自動辨識並移除圖片背景 |
| **技術重點** | 前景分割模型、邊緣羽化、髮絲處理 |
| **WASM 模組** | onnx-runtime-wasm + 去背模型 (~20 MB) |
| **執行流程** | 1. 上傳圖片 → 2. AI 分析 → 3. 移除背景 → 4. 輸出透明 PNG |
| **輸入格式** | PNG, JPG, WebP |
| **輸出格式** | PNG (透明背景) |
| **效能需求** | 記憶體：圖片大小 x5 + 模型、處理時間：3-10秒 |
| **進度狀態** | ✅ 已完成 |

---

## IMG-047 純色背景去除

| 欄位 | 內容 |
|------|------|
| **用途** | 移除單一純色背景（綠幕、白底等） |
| **技術重點** | 色彩範圍選取、容差調整、邊緣處理 |
| **WASM 模組** | photon-wasm (~2 MB) |
| **執行流程** | 1. 上傳圖片 → 2. 選取背景色 → 3. 調整容差 → 4. 輸出 |
| **輸入格式** | PNG, JPG, WebP |
| **輸出格式** | PNG (透明背景) |
| **效能需求** | 記憶體：圖片大小 x2、處理時間：<2秒 |
| **進度狀態** | ✅ 已完成 |

---

## IMG-048 更換背景

| 欄位 | 內容 |
|------|------|
| **用途** | 將去背後的圖片放置到新背景上 |
| **技術重點** | 圖層合成、邊緣融合、比例調整 |
| **WASM 模組** | photon-wasm (~2 MB) |
| **執行流程** | 1. 上傳前景圖 → 2. 上傳背景圖 → 3. 調整位置 → 4. 合成輸出 |
| **輸入格式** | PNG (透明), JPG |
| **輸出格式** | PNG, JPG |
| **效能需求** | 記憶體：兩圖大小總和 x2、處理時間：<2秒 |
| **進度狀態** | ✅ 已完成 |

---

## IMG-049 背景模糊

| 欄位 | 內容 |
|------|------|
| **用途** | 保留主體清晰，模糊背景（景深效果） |
| **技術重點** | 主體偵測、遮罩生成、高斯模糊 |
| **WASM 模組** | photon-wasm (~2 MB) + 分割模型 |
| **執行流程** | 1. 上傳圖片 → 2. 偵測主體 → 3. 模糊背景 → 4. 輸出 |
| **輸入格式** | PNG, JPG, WebP |
| **輸出格式** | PNG, JPG |
| **效能需求** | 記憶體：圖片大小 x4、處理時間：3-8秒 |
| **進度狀態** | ✅ 已完成 |

---

## IMG-050 批量去背

| 欄位 | 內容 |
|------|------|
| **用途** | 批量處理多張圖片的背景移除 |
| **技術重點** | 並行處理、進度追蹤、ZIP 打包 |
| **WASM 模組** | onnx-runtime-wasm + 去背模型 |
| **執行流程** | 1. 上傳多圖 → 2. 並行去背 → 3. 打包下載 |
| **輸入格式** | PNG, JPG |
| **輸出格式** | ZIP (含 PNG) |
| **效能需求** | 記憶體：依批量大小、處理時間：依數量 |
| **進度狀態** | ✅ 已完成 |

---

## IMG-051 多圖水平拼接

| 欄位 | 內容 |
|------|------|
| **用途** | 將多張圖片水平橫向拼接成一張 |
| **技術重點** | 尺寸對齊、間距設定、背景填充 |
| **WASM 模組** | 原生 Canvas API (無需 WASM) |
| **執行流程** | 1. 上傳多圖 → 2. 排列順序 → 3. 設定間距 → 4. 輸出 |
| **輸入格式** | PNG, JPG, WebP |
| **輸出格式** | PNG, JPG |
| **效能需求** | 記憶體：所有圖片總和 x2、處理時間：<3秒 |
| **進度狀態** | ✅ 已完成 |

---

## IMG-052 多圖垂直拼接

| 欄位 | 內容 |
|------|------|
| **用途** | 將多張圖片垂直縱向拼接成一張 |
| **技術重點** | 尺寸對齊、間距設定、自動縮放 |
| **WASM 模組** | 原生 Canvas API (無需 WASM) |
| **執行流程** | 1. 上傳多圖 → 2. 排列順序 → 3. 設定間距 → 4. 輸出 |
| **輸入格式** | PNG, JPG, WebP |
| **輸出格式** | PNG, JPG |
| **效能需求** | 記憶體：所有圖片總和 x2、處理時間：<3秒 |
| **進度狀態** | ⬜ 待開發 |

---

## IMG-053 九宮格拼圖

| 欄位 | 內容 |
|------|------|
| **用途** | 將多張圖片排列成九宮格（3x3）布局 |
| **技術重點** | 網格布局、自動裁切填充、間距控制 |
| **WASM 模組** | 原生 Canvas API (無需 WASM) |
| **執行流程** | 1. 上傳 9 張圖 → 2. 調整順序 → 3. 設定樣式 → 4. 輸出 |
| **輸入格式** | PNG, JPG, WebP |
| **輸出格式** | PNG, JPG |
| **效能需求** | 記憶體：9 張圖總和 x2、處理時間：<2秒 |
| **進度狀態** | ⬜ 待開發 |

---

## IMG-054 自訂網格拼圖

| 欄位 | 內容 |
|------|------|
| **用途** | 自訂行列數的網格圖片拼接 |
| **技術重點** | 動態網格生成、拖拽排序、單元格編輯 |
| **WASM 模組** | 原生 Canvas API (無需 WASM) |
| **執行流程** | 1. 設定網格大小 → 2. 上傳圖片 → 3. 排列 → 4. 輸出 |
| **輸入格式** | PNG, JPG, WebP |
| **輸出格式** | PNG, JPG |
| **效能需求** | 記憶體：所有圖片總和 x2、處理時間：<3秒 |
| **進度狀態** | ⬜ 待開發 |

---

## IMG-055 長截圖合併

| 欄位 | 內容 |
|------|------|
| **用途** | 合併多張連續截圖成一張長圖（對話記錄等） |
| **技術重點** | 重疊區域偵測、自動對齊、無縫拼接 |
| **WASM 模組** | photon-wasm (~2 MB) |
| **執行流程** | 1. 上傳多張截圖 → 2. 偵測重疊 → 3. 自動拼接 → 4. 輸出 |
| **輸入格式** | PNG, JPG |
| **輸出格式** | PNG, JPG |
| **效能需求** | 記憶體：所有圖片總和 x3、處理時間：3-10秒 |
| **進度狀態** | ⬜ 待開發 |

---

## IMG-056 照片蒙太奇

| 欄位 | 內容 |
|------|------|
| **用途** | 用多張小圖拼成一張大圖馬賽克效果 |
| **技術重點** | 色彩匹配、圖庫管理、密度調整 |
| **WASM 模組** | photon-wasm (~2 MB) |
| **執行流程** | 1. 上傳主圖 → 2. 上傳素材圖庫 → 3. 生成蒙太奇 → 4. 輸出 |
| **輸入格式** | PNG, JPG |
| **輸出格式** | PNG, JPG |
| **效能需求** | 記憶體：高（依素材數量）、處理時間：10-30秒 |
| **進度狀態** | ⬜ 待開發 |

---

## IMG-057 照片拼貼模板

| 欄位 | 內容 |
|------|------|
| **用途** | 使用預設模板製作照片拼貼 |
| **技術重點** | 模板引擎、拖放編輯、遮罩形狀 |
| **WASM 模組** | 原生 Canvas API (無需 WASM) |
| **執行流程** | 1. 選擇模板 → 2. 上傳圖片 → 3. 調整位置 → 4. 輸出 |
| **輸入格式** | PNG, JPG, WebP |
| **輸出格式** | PNG, JPG |
| **效能需求** | 記憶體：圖片總和 x2、處理時間：<2秒 |
| **進度狀態** | ⬜ 待開發 |

---

## IMG-058 證件照製作

| 欄位 | 內容 |
|------|------|
| **用途** | 製作標準證件照（1吋、2吋、護照等尺寸） |
| **技術重點** | 人臉偵測、自動裁切、背景替換、排版列印 |
| **WASM 模組** | photon-wasm (~2 MB) + 人臉偵測模型 |
| **執行流程** | 1. 上傳照片 → 2. 偵測人臉 → 3. 裁切調整 → 4. 排版輸出 |
| **輸入格式** | PNG, JPG |
| **輸出格式** | JPG (列印用) |
| **效能需求** | 記憶體：圖片大小 x3、處理時間：2-5秒 |
| **進度狀態** | ⬜ 待開發 |

---

## IMG-059 圖片比較滑桿

| 欄位 | 內容 |
|------|------|
| **用途** | 建立前後對比滑桿效果圖（before/after） |
| **技術重點** | 雙圖疊加、滑桿互動、嵌入式 HTML |
| **WASM 模組** | 原生 Canvas API (無需 WASM) |
| **執行流程** | 1. 上傳兩張圖 → 2. 對齊調整 → 3. 輸出互動式 HTML |
| **輸入格式** | PNG, JPG |
| **輸出格式** | HTML, PNG (靜態) |
| **效能需求** | 記憶體：兩圖大小 x2、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## IMG-060 圖片差異比較

| 欄位 | 內容 |
|------|------|
| **用途** | 比較兩張圖片的差異並高亮顯示不同處 |
| **技術重點** | 像素比對、差異遮罩、敏感度調整 |
| **WASM 模組** | photon-wasm (~2 MB) |
| **執行流程** | 1. 上傳兩張圖 → 2. 分析差異 → 3. 生成差異圖 → 4. 輸出 |
| **輸入格式** | PNG, JPG |
| **輸出格式** | PNG |
| **效能需求** | 記憶體：兩圖大小 x3、處理時間：1-3秒 |
| **進度狀態** | ⬜ 待開發 |

---

## 下一個檔案

IMG-061 ~ IMG-080 請參閱 [plan1-4.md](plan1-4.md)
