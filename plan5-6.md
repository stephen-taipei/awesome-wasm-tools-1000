# Plan 5-6: 文字分析工具 (TXT-101 ~ TXT-120)

> 共 150 個工具，本檔案包含 TXT-101 ~ TXT-120

---

## TXT-101 TODO 提取

| 欄位 | 內容 |
|------|------|
| **用途** | 從程式碼中提取 TODO/FIXME 標記 |
| **技術重點** | 多關鍵字支援、位置記錄、分類統計 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 上傳程式碼 → 2. 掃描 → 3. 列出所有 TODO |
| **輸入格式** | 程式碼檔案 |
| **輸出格式** | TODO 列表 |
| **效能需求** | 記憶體：低、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-102 License 識別

| 欄位 | 內容 |
|------|------|
| **用途** | 識別程式碼的授權條款 |
| **技術重點** | License 指紋、SPDX 識別碼 |
| **WASM 模組** | licensee-wasm (~500 KB) |
| **執行流程** | 1. 上傳 LICENSE 檔案 → 2. 分析 → 3. 顯示授權類型 |
| **輸入格式** | LICENSE 文字 |
| **輸出格式** | 授權類型、SPDX ID |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-103 URL 解析

| 欄位 | 內容 |
|------|------|
| **用途** | 解析並分析 URL 結構 |
| **技術重點** | 協議、主機、路徑、查詢參數、片段 |
| **WASM 模組** | 原生 JS (URL API) |
| **執行流程** | 1. 輸入 URL → 2. 解析 → 3. 顯示各部分 |
| **輸入格式** | URL |
| **輸出格式** | 結構化資訊 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-104 URL 編碼/解碼

| 欄位 | 內容 |
|------|------|
| **用途** | URL 百分比編碼轉換 |
| **技術重點** | encodeURI、encodeURIComponent、UTF-8 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入文字 → 2. 選擇編碼/解碼 → 3. 轉換 → 4. 複製 |
| **輸入格式** | 文字或 URL |
| **輸出格式** | 編碼/解碼後文字 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-105 URL 批次轉換

| 欄位 | 內容 |
|------|------|
| **用途** | 批次處理多個 URL |
| **技術重點** | 添加/移除參數、替換域名、修改路徑 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入 URL 列表 → 2. 設定規則 → 3. 批次轉換 → 4. 匯出 |
| **輸入格式** | URL 列表 |
| **輸出格式** | 轉換後 URL 列表 |
| **效能需求** | 記憶體：低、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-106 Email 驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證 Email 地址格式 |
| **技術重點** | RFC 5322 驗證、常見錯誤檢測 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入 Email → 2. 驗證 → 3. 顯示結果 |
| **輸入格式** | Email 地址 |
| **輸出格式** | 驗證結果 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-107 Email 批次驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 批次驗證多個 Email 地址 |
| **技術重點** | 批次處理、錯誤分類、匯出報告 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 上傳 Email 列表 → 2. 批次驗證 → 3. 匯出結果 |
| **輸入格式** | Email 列表 (CSV) |
| **輸出格式** | 驗證報告 |
| **效能需求** | 記憶體：依數量、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-108 電話號碼格式化

| 欄位 | 內容 |
|------|------|
| **用途** | 格式化並驗證電話號碼 |
| **技術重點** | libphonenumber、國際格式、E.164 |
| **WASM 模組** | libphonenumber-wasm (~2 MB) |
| **執行流程** | 1. 輸入電話號碼 → 2. 選擇國家 → 3. 格式化 → 4. 驗證 |
| **輸入格式** | 電話號碼 |
| **輸出格式** | 格式化號碼、驗證結果 |
| **效能需求** | 記憶體：中等、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-109 信用卡驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證信用卡號碼格式 |
| **技術重點** | Luhn 算法、發卡行識別 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入卡號 → 2. 驗證 → 3. 顯示卡類型 |
| **輸入格式** | 信用卡號碼 |
| **輸出格式** | 驗證結果、卡類型 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-110 身份證驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證身份證號碼格式 |
| **技術重點** | 多國格式、校驗碼驗證 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入身份證號 → 2. 選擇國家 → 3. 驗證 |
| **輸入格式** | 身份證號碼 |
| **輸出格式** | 驗證結果 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-111 統一編號驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證台灣統一編號 |
| **技術重點** | 統編校驗碼規則 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入統一編號 → 2. 驗證 → 3. 顯示結果 |
| **輸入格式** | 8 位數字 |
| **輸出格式** | 驗證結果 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-112 IBAN 驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證國際銀行帳號 |
| **技術重點** | IBAN 格式、國家規則、校驗碼 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入 IBAN → 2. 驗證 → 3. 解析銀行資訊 |
| **輸入格式** | IBAN |
| **輸出格式** | 驗證結果、銀行資訊 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-113 ISBN 驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證並轉換 ISBN |
| **技術重點** | ISBN-10/13、校驗碼、互轉 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入 ISBN → 2. 驗證 → 3. 轉換版本 |
| **輸入格式** | ISBN |
| **輸出格式** | 驗證結果、轉換後 ISBN |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-114 條碼資料解析

| 欄位 | 內容 |
|------|------|
| **用途** | 解析條碼資料內容 |
| **技術重點** | EAN、UPC、Code128、QR 內容解析 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入條碼資料 → 2. 識別格式 → 3. 解析內容 |
| **輸入格式** | 條碼字串 |
| **輸出格式** | 解析結果 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-115 User Agent 解析

| 欄位 | 內容 |
|------|------|
| **用途** | 解析 User Agent 字串 |
| **技術重點** | 瀏覽器、作業系統、裝置識別 |
| **WASM 模組** | ua-parser-wasm (~300 KB) |
| **執行流程** | 1. 輸入 UA 字串 → 2. 解析 → 3. 顯示詳細資訊 |
| **輸入格式** | User Agent 字串 |
| **輸出格式** | 瀏覽器/OS/裝置資訊 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-116 IP 地址解析

| 欄位 | 內容 |
|------|------|
| **用途** | 解析並驗證 IP 地址 |
| **技術重點** | IPv4/IPv6、CIDR、子網計算 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入 IP → 2. 解析 → 3. 顯示詳細資訊 |
| **輸入格式** | IP 地址 |
| **輸出格式** | 解析結果、子網資訊 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-117 MAC 地址解析

| 欄位 | 內容 |
|------|------|
| **用途** | 解析 MAC 地址並識別製造商 |
| **技術重點** | OUI 查詢、格式轉換 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入 MAC → 2. 查詢 OUI → 3. 顯示製造商 |
| **輸入格式** | MAC 地址 |
| **輸出格式** | 製造商資訊 |
| **效能需求** | 記憶體：中等、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-118 HTTP Header 解析

| 欄位 | 內容 |
|------|------|
| **用途** | 解析並說明 HTTP Header |
| **技術重點** | 標頭解析、RFC 說明、安全建議 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入 Headers → 2. 解析 → 3. 顯示說明 |
| **輸入格式** | HTTP Header 文字 |
| **輸出格式** | 結構化說明 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-119 JWT Token 分析

| 欄位 | 內容 |
|------|------|
| **用途** | 分析 JWT Token 結構與內容 |
| **技術重點** | Base64 解碼、過期檢查、聲明分析 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入 JWT → 2. 解碼 → 3. 顯示 Header/Payload → 4. 檢查過期 |
| **輸入格式** | JWT 字串 |
| **輸出格式** | 結構化內容、過期狀態 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-120 Cookie 解析

| 欄位 | 內容 |
|------|------|
| **用途** | 解析並分析 Cookie 字串 |
| **技術重點** | 屬性解析、安全檢查、過期計算 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入 Cookie → 2. 解析 → 3. 顯示屬性 → 4. 安全建議 |
| **輸入格式** | Cookie 字串 |
| **輸出格式** | 結構化資訊 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## 下一個檔案

TXT-121 ~ TXT-140 請參閱 [plan5-7.md](plan5-7.md)
