# Plan 5-3: 文字分析工具 (TXT-041 ~ TXT-060)

> 共 150 個工具，本檔案包含 TXT-041 ~ TXT-060

---

## TXT-041 富文字轉純文字

| 欄位 | 內容 |
|------|------|
| **用途** | 移除格式，提取純文字 |
| **技術重點** | HTML/RTF 解析、格式清除、保留結構 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 貼上富文字 → 2. 處理 → 3. 輸出純文字 |
| **輸入格式** | 富文字 (HTML/RTF) |
| **輸出格式** | 純文字 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-042 BBCode 轉換

| 欄位 | 內容 |
|------|------|
| **用途** | BBCode 與 HTML 互轉 |
| **技術重點** | BBCode 解析、HTML 生成 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入文字 → 2. 選擇轉換方向 → 3. 轉換 → 4. 輸出 |
| **輸入格式** | BBCode 或 HTML |
| **輸出格式** | HTML 或 BBCode |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-043 Wiki 語法轉換

| 欄位 | 內容 |
|------|------|
| **用途** | Wiki 標記語言轉換 |
| **技術重點** | MediaWiki、WikiText 解析 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入 Wiki 語法 → 2. 轉換 → 3. 輸出 HTML/Markdown |
| **輸入格式** | Wiki 語法 |
| **輸出格式** | HTML/Markdown |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-044 reStructuredText 轉換

| 欄位 | 內容 |
|------|------|
| **用途** | reStructuredText (RST) 轉 HTML |
| **技術重點** | RST 解析、指令處理 |
| **WASM 模組** | rst-wasm (~400 KB) |
| **執行流程** | 1. 輸入 RST → 2. 轉換 → 3. 預覽/下載 HTML |
| **輸入格式** | reStructuredText |
| **輸出格式** | HTML |
| **效能需求** | 記憶體：中等、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-045 AsciiDoc 轉換

| 欄位 | 內容 |
|------|------|
| **用途** | AsciiDoc 轉 HTML |
| **技術重點** | AsciiDoc 解析、完整支援 |
| **WASM 模組** | asciidoctor-wasm (~1 MB) |
| **執行流程** | 1. 輸入 AsciiDoc → 2. 轉換 → 3. 預覽/下載 HTML |
| **輸入格式** | AsciiDoc |
| **輸出格式** | HTML |
| **效能需求** | 記憶體：中等、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-046 LaTeX 公式編輯

| 欄位 | 內容 |
|------|------|
| **用途** | 編輯並預覽 LaTeX 數學公式 |
| **技術重點** | KaTeX/MathJax、即時渲染 |
| **WASM 模組** | katex-wasm (~500 KB) |
| **執行流程** | 1. 輸入 LaTeX → 2. 即時預覽 → 3. 複製/匯出 |
| **輸入格式** | LaTeX 數學語法 |
| **輸出格式** | 渲染圖片/SVG |
| **效能需求** | 記憶體：中等、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-047 LaTeX 轉 MathML

| 欄位 | 內容 |
|------|------|
| **用途** | 將 LaTeX 公式轉為 MathML |
| **技術重點** | 語法轉換、無障礙支援 |
| **WASM 模組** | katex-wasm (~500 KB) |
| **執行流程** | 1. 輸入 LaTeX → 2. 轉換 → 3. 輸出 MathML |
| **輸入格式** | LaTeX |
| **輸出格式** | MathML |
| **效能需求** | 記憶體：中等、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-048 LaTeX 文件編輯

| 欄位 | 內容 |
|------|------|
| **用途** | 完整 LaTeX 文件編輯與預覽 |
| **技術重點** | TeXLive 子集、PDF 輸出 |
| **WASM 模組** | texlive-wasm (~50 MB) |
| **執行流程** | 1. 編輯 LaTeX → 2. 編譯 → 3. 預覽 PDF |
| **輸入格式** | LaTeX 文件 |
| **輸出格式** | PDF |
| **效能需求** | 記憶體：高、處理時間：較長 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-049 CSV 編輯器

| 欄位 | 內容 |
|------|------|
| **用途** | 編輯 CSV 檔案 |
| **技術重點** | 表格介面、排序過濾、公式 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 上傳 CSV → 2. 表格編輯 → 3. 下載 |
| **輸入格式** | CSV |
| **輸出格式** | CSV |
| **效能需求** | 記憶體：依資料量、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-050 CSV ↔ JSON 轉換

| 欄位 | 內容 |
|------|------|
| **用途** | CSV 與 JSON 格式互轉 |
| **技術重點** | 欄位對應、巢狀結構、陣列處理 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 上傳檔案 → 2. 選擇轉換方向 → 3. 設定對應 → 4. 下載 |
| **輸入格式** | CSV 或 JSON |
| **輸出格式** | JSON 或 CSV |
| **效能需求** | 記憶體：依資料量、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-051 CSV ↔ XML 轉換

| 欄位 | 內容 |
|------|------|
| **用途** | CSV 與 XML 格式互轉 |
| **技術重點** | 元素/屬性對應、命名空間 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 上傳檔案 → 2. 設定對應 → 3. 轉換 → 4. 下載 |
| **輸入格式** | CSV 或 XML |
| **輸出格式** | XML 或 CSV |
| **效能需求** | 記憶體：依資料量、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-052 JSON 格式化

| 欄位 | 內容 |
|------|------|
| **用途** | 格式化並美化 JSON |
| **技術重點** | 縮排設定、排序、壓縮 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入 JSON → 2. 選擇格式 → 3. 格式化 → 4. 複製 |
| **輸入格式** | JSON |
| **輸出格式** | 格式化 JSON |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-053 JSON 驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證 JSON 語法與 Schema |
| **技術重點** | 語法檢查、JSON Schema 驗證 |
| **WASM 模組** | ajv-wasm (~400 KB) |
| **執行流程** | 1. 輸入 JSON → 2. 上傳 Schema → 3. 驗證 → 4. 顯示錯誤 |
| **輸入格式** | JSON、JSON Schema |
| **輸出格式** | 驗證結果 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-054 JSON 路徑查詢

| 欄位 | 內容 |
|------|------|
| **用途** | 使用 JSONPath 查詢 JSON |
| **技術重點** | JSONPath 語法、結果高亮 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入 JSON → 2. 輸入 JSONPath → 3. 查詢 → 4. 顯示結果 |
| **輸入格式** | JSON、JSONPath |
| **輸出格式** | 查詢結果 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-055 JSON ↔ YAML 轉換

| 欄位 | 內容 |
|------|------|
| **用途** | JSON 與 YAML 格式互轉 |
| **技術重點** | YAML 解析、多文件支援 |
| **WASM 模組** | yaml-wasm (~200 KB) |
| **執行流程** | 1. 輸入文字 → 2. 選擇轉換方向 → 3. 轉換 → 4. 複製 |
| **輸入格式** | JSON 或 YAML |
| **輸出格式** | YAML 或 JSON |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-056 YAML 驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證 YAML 語法 |
| **技術重點** | YAML 1.2 語法、錯誤定位 |
| **WASM 模組** | yaml-wasm (~200 KB) |
| **執行流程** | 1. 輸入 YAML → 2. 驗證 → 3. 顯示錯誤/成功 |
| **輸入格式** | YAML |
| **輸出格式** | 驗證結果 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-057 TOML 編輯器

| 欄位 | 內容 |
|------|------|
| **用途** | 編輯並驗證 TOML |
| **技術重點** | TOML 解析、語法高亮 |
| **WASM 模組** | toml-wasm (~150 KB) |
| **執行流程** | 1. 輸入 TOML → 2. 編輯 → 3. 驗證 → 4. 下載 |
| **輸入格式** | TOML |
| **輸出格式** | TOML |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-058 XML 格式化

| 欄位 | 內容 |
|------|------|
| **用途** | 格式化並美化 XML |
| **技術重點** | 縮排、屬性換行、命名空間 |
| **WASM 模組** | 原生 JS (DOMParser) |
| **執行流程** | 1. 輸入 XML → 2. 選擇格式 → 3. 格式化 → 4. 複製 |
| **輸入格式** | XML |
| **輸出格式** | 格式化 XML |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-059 XML 驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證 XML 語法與 Schema |
| **技術重點** | 良構性檢查、XSD 驗證 |
| **WASM 模組** | libxml2-wasm (~1 MB) |
| **執行流程** | 1. 輸入 XML → 2. 上傳 XSD → 3. 驗證 → 4. 顯示結果 |
| **輸入格式** | XML、XSD |
| **輸出格式** | 驗證結果 |
| **效能需求** | 記憶體：中等、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-060 XPath 查詢

| 欄位 | 內容 |
|------|------|
| **用途** | 使用 XPath 查詢 XML |
| **技術重點** | XPath 1.0/2.0、結果高亮 |
| **WASM 模組** | 原生 JS (XPathEvaluator) |
| **執行流程** | 1. 輸入 XML → 2. 輸入 XPath → 3. 查詢 → 4. 顯示結果 |
| **輸入格式** | XML、XPath |
| **輸出格式** | 查詢結果 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## 下一個檔案

TXT-061 ~ TXT-080 請參閱 [plan5-4.md](plan5-4.md)
