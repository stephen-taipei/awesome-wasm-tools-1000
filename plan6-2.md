# Plan 6-2: PDF 文件工具 (PDF-021 ~ PDF-040)

> 共 120 個工具，本檔案包含 PDF-021 ~ PDF-040

---

## PDF-021 PDF 加密

| 欄位 | 內容 |
|------|------|
| **用途** | 為 PDF 添加密碼保護 |
| **技術重點** | AES-256 加密、使用者/擁有者密碼、權限控制 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 設定密碼 → 3. 設定權限 → 4. 加密 → 5. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（加密） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-022 PDF 解密

| 欄位 | 內容 |
|------|------|
| **用途** | 移除 PDF 密碼保護 |
| **技術重點** | 密碼驗證、解密處理 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳加密 PDF → 2. 輸入密碼 → 3. 解密 → 4. 下載 |
| **輸入格式** | PDF（加密） |
| **輸出格式** | PDF（無密碼） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-023 PDF 權限修改

| 欄位 | 內容 |
|------|------|
| **用途** | 修改 PDF 權限設定 |
| **技術重點** | 列印/複製/修改/註解權限 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 設定權限 → 3. 儲存 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（權限已修改） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-024 PDF 數位簽章

| 欄位 | 內容 |
|------|------|
| **用途** | 在 PDF 上添加數位簽章 |
| **技術重點** | X.509 憑證、PKCS#7 簽章、時間戳 |
| **WASM 模組** | pdf-lib-wasm + pkijs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 上傳憑證 → 3. 簽章 → 4. 下載 |
| **輸入格式** | PDF、P12 憑證 |
| **輸出格式** | PDF（已簽章） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-025 PDF 簽章驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證 PDF 數位簽章 |
| **技術重點** | 簽章驗證、憑證鏈驗證、完整性檢查 |
| **WASM 模組** | pdfjs-wasm + pkijs-wasm (~5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 驗證簽章 → 3. 顯示結果 |
| **輸入格式** | PDF（已簽章） |
| **輸出格式** | 驗證報告 |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-026 PDF 浮水印添加

| 欄位 | 內容 |
|------|------|
| **用途** | 在 PDF 頁面添加浮水印 |
| **技術重點** | 文字/圖片浮水印、透明度、位置、旋轉 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 設定浮水印 → 3. 預覽 → 4. 套用 → 5. 下載 |
| **輸入格式** | PDF、浮水印內容 |
| **輸出格式** | PDF（含浮水印） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-027 PDF 浮水印移除

| 欄位 | 內容 |
|------|------|
| **用途** | 嘗試移除 PDF 浮水印 |
| **技術重點** | 圖層分析、特定元素移除 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 偵測浮水印 → 3. 移除 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（浮水印已移除） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-028 PDF 頁首頁尾

| 欄位 | 內容 |
|------|------|
| **用途** | 添加頁首頁尾（頁碼、日期等） |
| **技術重點** | 位置設定、字型、頁碼格式 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 設定內容 → 3. 預覽 → 4. 套用 → 5. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（含頁首頁尾） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-029 PDF 貝茲印章

| 欄位 | 內容 |
|------|------|
| **用途** | 添加貝茲印章（Bates Stamp） |
| **技術重點** | 連續編號、前綴後綴、位置 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 設定編號格式 → 3. 套用 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（含 Bates 編號） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-030 PDF 書籤編輯

| 欄位 | 內容 |
|------|------|
| **用途** | 編輯 PDF 書籤/大綱 |
| **技術重點** | 書籤新增/刪除/修改、階層結構、連結目標 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 編輯書籤 → 3. 儲存 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（書籤已編輯） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-031 PDF 自動建立書籤

| 欄位 | 內容 |
|------|------|
| **用途** | 根據標題自動建立書籤 |
| **技術重點** | 標題偵測、階層識別、自動連結 |
| **WASM 模組** | pdfjs-wasm + pdf-lib-wasm (~4.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 偵測標題 → 3. 建立書籤 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（含自動書籤） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-032 PDF 連結編輯

| 欄位 | 內容 |
|------|------|
| **用途** | 添加或編輯 PDF 中的連結 |
| **技術重點** | 網址連結、頁面連結、熱區設定 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇區域 → 3. 設定連結 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（含連結） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-033 PDF 元資料編輯

| 欄位 | 內容 |
|------|------|
| **用途** | 編輯 PDF 元資料 |
| **技術重點** | 標題、作者、關鍵字、建立日期 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 編輯元資料 → 3. 儲存 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（元資料已編輯） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-034 PDF 元資料清除

| 欄位 | 內容 |
|------|------|
| **用途** | 清除 PDF 中的敏感元資料 |
| **技術重點** | XMP 清除、自訂屬性移除、隱私保護 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇清除項目 → 3. 清除 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（元資料已清除） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-035 PDF 資訊檢視

| 欄位 | 內容 |
|------|------|
| **用途** | 檢視 PDF 詳細資訊 |
| **技術重點** | 元資料、頁數、版本、加密狀態、字型 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 分析 → 3. 顯示資訊 |
| **輸入格式** | PDF |
| **輸出格式** | 資訊報告 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-036 PDF 結構檢視

| 欄位 | 內容 |
|------|------|
| **用途** | 檢視 PDF 內部結構 |
| **技術重點** | 物件樹、交叉參考表、串流內容 |
| **WASM 模組** | pdfjs-wasm (~3 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 解析結構 → 3. 樹狀顯示 |
| **輸入格式** | PDF |
| **輸出格式** | 結構樹 |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-037 PDF 頁面大小調整

| 欄位 | 內容 |
|------|------|
| **用途** | 調整 PDF 頁面尺寸 |
| **技術重點** | 縮放、裁切、邊距、標準尺寸 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇尺寸 → 3. 調整 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（尺寸已調整） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-038 PDF 頁面裁切

| 欄位 | 內容 |
|------|------|
| **用途** | 裁切 PDF 頁面邊緣 |
| **技術重點** | CropBox、MediaBox、BleedBox |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 設定裁切區域 → 3. 裁切 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（已裁切） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-039 PDF N 合 1

| 欄位 | 內容 |
|------|------|
| **用途** | 多頁合併到一頁 |
| **技術重點** | 2/4/6/9 合 1、排列順序、邊框 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 選擇合併數 → 3. 排列 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（多頁合一） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## PDF-040 PDF 海報分割

| 欄位 | 內容 |
|------|------|
| **用途** | 將大頁面分割為多頁 |
| **技術重點** | 分割格線、重疊區域、頁面編號 |
| **WASM 模組** | pdf-lib-wasm (~1.5 MB) |
| **執行流程** | 1. 上傳 PDF → 2. 設定分割數 → 3. 分割 → 4. 下載 |
| **輸入格式** | PDF |
| **輸出格式** | PDF（已分割） |
| **效能需求** | 記憶體：PDF 大小 x2、處理時間：依頁數 |
| **進度狀態** | ⬜ 待開發 |

---

## 下一個檔案

PDF-041 ~ PDF-060 請參閱 [plan6-3.md](plan6-3.md)
