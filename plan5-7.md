# Plan 5-7: 文字分析工具 (TXT-121 ~ TXT-140)

> 共 150 個工具，本檔案包含 TXT-121 ~ TXT-140

---

## TXT-121 SQL 注入檢測

| 欄位 | 內容 |
|------|------|
| **用途** | 檢測文字中的 SQL 注入模式 |
| **技術重點** | 常見攻擊模式、風險評估 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入文字 → 2. 掃描 → 3. 標示可疑內容 |
| **輸入格式** | 文字 |
| **輸出格式** | 檢測報告 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-122 XSS 檢測

| 欄位 | 內容 |
|------|------|
| **用途** | 檢測文字中的 XSS 攻擊模式 |
| **技術重點** | 腳本注入、事件處理器、編碼繞過 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入文字 → 2. 掃描 → 3. 標示可疑內容 |
| **輸入格式** | 文字/HTML |
| **輸出格式** | 檢測報告 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-123 敏感資訊偵測

| 欄位 | 內容 |
|------|------|
| **用途** | 偵測文字中的敏感資訊 |
| **技術重點** | API 金鑰、密碼、個資、信用卡號 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入文字 → 2. 掃描 → 3. 標示敏感內容 |
| **輸入格式** | 文字/程式碼 |
| **輸出格式** | 偵測報告 |
| **效能需求** | 記憶體：低、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-124 文字脫敏

| 欄位 | 內容 |
|------|------|
| **用途** | 自動遮蔽敏感資訊 |
| **技術重點** | 姓名/電話/Email/身份證遮蔽、自訂規則 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入文字 → 2. 選擇遮蔽類型 → 3. 處理 → 4. 輸出 |
| **輸入格式** | 文字 |
| **輸出格式** | 脫敏後文字 |
| **效能需求** | 記憶體：低、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-125 文字提取（從 HTML）

| 欄位 | 內容 |
|------|------|
| **用途** | 從 HTML 提取純文字內容 |
| **技術重點** | DOM 解析、標籤移除、結構保留 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入 HTML → 2. 提取 → 3. 輸出純文字 |
| **輸入格式** | HTML |
| **輸出格式** | 純文字 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-126 連結提取

| 欄位 | 內容 |
|------|------|
| **用途** | 從文字中提取所有連結 |
| **技術重點** | URL 正規表達式、去重、分類 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入文字 → 2. 提取 → 3. 列出連結 |
| **輸入格式** | 文字/HTML |
| **輸出格式** | 連結列表 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-127 Email 提取

| 欄位 | 內容 |
|------|------|
| **用途** | 從文字中提取所有 Email 地址 |
| **技術重點** | Email 正規表達式、去重、驗證 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入文字 → 2. 提取 → 3. 列出 Email |
| **輸入格式** | 文字 |
| **輸出格式** | Email 列表 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-128 電話號碼提取

| 欄位 | 內容 |
|------|------|
| **用途** | 從文字中提取電話號碼 |
| **技術重點** | 多格式識別、國際號碼、去重 |
| **WASM 模組** | libphonenumber-wasm (~2 MB) |
| **執行流程** | 1. 輸入文字 → 2. 選擇國家 → 3. 提取 → 4. 列出號碼 |
| **輸入格式** | 文字 |
| **輸出格式** | 電話號碼列表 |
| **效能需求** | 記憶體：中等、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-129 日期時間提取

| 欄位 | 內容 |
|------|------|
| **用途** | 從文字中提取日期時間 |
| **技術重點** | 多格式識別、自然語言日期 |
| **WASM 模組** | chrono-wasm (~400 KB) |
| **執行流程** | 1. 輸入文字 → 2. 提取 → 3. 列出日期時間 |
| **輸入格式** | 文字 |
| **輸出格式** | 日期時間列表 |
| **效能需求** | 記憶體：中等、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-130 數字提取

| 欄位 | 內容 |
|------|------|
| **用途** | 從文字中提取所有數字 |
| **技術重點** | 整數/小數/負數、貨幣、百分比 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入文字 → 2. 選擇類型 → 3. 提取 → 4. 列出數字 |
| **輸入格式** | 文字 |
| **輸出格式** | 數字列表 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-131 Hashtag 提取

| 欄位 | 內容 |
|------|------|
| **用途** | 從文字中提取 Hashtag |
| **技術重點** | #tag 識別、統計、排序 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入文字 → 2. 提取 → 3. 列出並統計 |
| **輸入格式** | 文字 |
| **輸出格式** | Hashtag 列表（含頻率） |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-132 @提及提取

| 欄位 | 內容 |
|------|------|
| **用途** | 從文字中提取 @提及 |
| **技術重點** | @username 識別、統計 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入文字 → 2. 提取 → 3. 列出提及 |
| **輸入格式** | 文字 |
| **輸出格式** | 提及列表 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-133 Git Diff 檢視器

| 欄位 | 內容 |
|------|------|
| **用途** | 視覺化顯示 Git Diff |
| **技術重點** | Unified Diff 解析、語法高亮、行內差異 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入 Diff → 2. 解析 → 3. 視覺化顯示 |
| **輸入格式** | Git Diff 文字 |
| **輸出格式** | 視覺化檢視 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-134 Patch 應用工具

| 欄位 | 內容 |
|------|------|
| **用途** | 將 Patch 套用到文字 |
| **技術重點** | Unified Diff 套用、衝突處理 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入原始文字 → 2. 輸入 Patch → 3. 套用 → 4. 輸出結果 |
| **輸入格式** | 文字、Patch |
| **輸出格式** | 更新後文字 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-135 文字版本比較

| 欄位 | 內容 |
|------|------|
| **用途** | 三方文字比較與合併 |
| **技術重點** | 三方 Diff、自動合併、衝突標記 |
| **WASM 模組** | diff3-wasm (~200 KB) |
| **執行流程** | 1. 輸入三個版本 → 2. 比較 → 3. 合併 → 4. 處理衝突 |
| **輸入格式** | 三段文字 |
| **輸出格式** | 合併結果 |
| **效能需求** | 記憶體：依文字量、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-136 Changelog 生成器

| 欄位 | 內容 |
|------|------|
| **用途** | 從 Git 提交生成 Changelog |
| **技術重點** | Conventional Commits、分類、版本分組 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入提交記錄 → 2. 解析 → 3. 生成 Changelog |
| **輸入格式** | Git Log 格式 |
| **輸出格式** | Markdown Changelog |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-137 語意化版本計算

| 欄位 | 內容 |
|------|------|
| **用途** | 計算並比較語意化版本 |
| **技術重點** | SemVer 解析、比較、範圍計算 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入版本 → 2. 選擇操作 → 3. 計算結果 |
| **輸入格式** | 版本號 |
| **輸出格式** | 計算結果 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-138 文字統計儀表板

| 欄位 | 內容 |
|------|------|
| **用途** | 綜合文字統計分析 |
| **技術重點** | 多維度統計、圖表展示 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入文字 → 2. 分析 → 3. 顯示儀表板 |
| **輸入格式** | 文字 |
| **輸出格式** | 統計儀表板 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-139 多檔案文字合併

| 欄位 | 內容 |
|------|------|
| **用途** | 合併多個文字檔案 |
| **技術重點** | 順序控制、分隔符、編碼處理 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 上傳多檔案 → 2. 排序 → 3. 設定分隔 → 4. 合併下載 |
| **輸入格式** | 多個文字檔 |
| **輸出格式** | 合併後文字檔 |
| **效能需求** | 記憶體：依總大小、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## TXT-140 文字分割工具

| 欄位 | 內容 |
|------|------|
| **用途** | 將大文字檔分割為多個小檔 |
| **技術重點** | 按行/字元/大小分割、保持完整句子 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 上傳檔案 → 2. 選擇分割方式 → 3. 分割 → 4. 下載 |
| **輸入格式** | 大文字檔 |
| **輸出格式** | 多個小文字檔 |
| **效能需求** | 記憶體：依檔案大小、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## 下一個檔案

TXT-141 ~ TXT-150 請參閱 [plan5-8.md](plan5-8.md)
