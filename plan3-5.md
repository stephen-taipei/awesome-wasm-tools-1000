# Plan 3-5: 壓縮轉檔工具 (CMP-081 ~ CMP-100)

> 共 120 個工具，本檔案包含 CMP-081 ~ CMP-100

---

## CMP-081 壓縮檔差異比對

| 欄位 | 內容 |
|------|------|
| **用途** | 比對兩個壓縮檔的內容差異 |
| **技術重點** | 目錄比對、檔案雜湊、差異報告 |
| **WASM 模組** | libarchive-wasm (~3 MB) |
| **執行流程** | 1. 上傳兩個壓縮檔 → 2. 分析差異 → 3. 顯示報告 |
| **輸入格式** | ZIP, 7Z |
| **輸出格式** | 差異報告 |
| **效能需求** | 記憶體：兩檔大小、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-082 壓縮檔合併

| 欄位 | 內容 |
|------|------|
| **用途** | 將多個壓縮檔合併為一個 |
| **技術重點** | 解壓合併、衝突處理、目錄結構 |
| **WASM 模組** | zip-wasm (~800 KB) |
| **執行流程** | 1. 上傳多個壓縮檔 → 2. 設定合併規則 → 3. 合併 → 4. 下載 |
| **輸入格式** | ZIP |
| **輸出格式** | ZIP |
| **效能需求** | 記憶體：所有檔案總和、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-083 增量壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 只壓縮與基準版本不同的檔案 |
| **技術重點** | 差異偵測、增量檔案、還原機制 |
| **WASM 模組** | zip-wasm (~800 KB) |
| **執行流程** | 1. 上傳基準壓縮檔 → 2. 上傳新檔案 → 3. 增量壓縮 → 4. 下載 |
| **輸入格式** | ZIP + 新檔案 |
| **輸出格式** | ZIP (增量) |
| **效能需求** | 記憶體：依差異大小、處理時間：依差異量 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-084 檔案雜湊計算

| 欄位 | 內容 |
|------|------|
| **用途** | 計算檔案的各種雜湊值 |
| **技術重點** | MD5、SHA-1、SHA-256、CRC32 |
| **WASM 模組** | 原生 SubtleCrypto API (無需 WASM) |
| **執行流程** | 1. 上傳檔案 → 2. 選擇雜湊類型 → 3. 計算 → 4. 顯示結果 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | 雜湊值 |
| **效能需求** | 記憶體：串流處理、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-085 檔案雜湊驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 驗證檔案雜湊值是否正確 |
| **技術重點** | 雜湊比對、多格式支援 |
| **WASM 模組** | 原生 SubtleCrypto API (無需 WASM) |
| **執行流程** | 1. 上傳檔案 → 2. 輸入預期雜湊 → 3. 驗證 → 4. 顯示結果 |
| **輸入格式** | 任意檔案 + 雜湊值 |
| **輸出格式** | 驗證結果 |
| **效能需求** | 記憶體：串流處理、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-086 校驗碼檔案生成

| 欄位 | 內容 |
|------|------|
| **用途** | 生成 MD5SUMS、SHA256SUMS 校驗檔 |
| **技術重點** | 批量計算、標準格式輸出 |
| **WASM 模組** | 原生 SubtleCrypto API (無需 WASM) |
| **執行流程** | 1. 上傳多檔案 → 2. 選擇雜湊類型 → 3. 生成校驗檔 → 4. 下載 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | MD5SUMS, SHA256SUMS |
| **效能需求** | 記憶體：串流處理、處理時間：依總大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-087 校驗碼檔案驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 使用校驗檔驗證多個檔案 |
| **技術重點** | 校驗檔解析、批量驗證、報告生成 |
| **WASM 模組** | 原生 SubtleCrypto API (無需 WASM) |
| **執行流程** | 1. 上傳校驗檔 → 2. 上傳待驗證檔案 → 3. 驗證 → 4. 顯示報告 |
| **輸入格式** | MD5SUMS/SHA256SUMS + 檔案 |
| **輸出格式** | 驗證報告 |
| **效能需求** | 記憶體：串流處理、處理時間：依總大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-088 SFV 校驗檔

| 欄位 | 內容 |
|------|------|
| **用途** | 生成和驗證 SFV (Simple File Verification) 檔案 |
| **技術重點** | CRC32 計算、SFV 格式 |
| **WASM 模組** | 原生 (無需 WASM) |
| **執行流程** | 1. 上傳檔案 → 2. 生成/驗證 SFV → 3. 下載/顯示結果 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | SFV |
| **效能需求** | 記憶體：串流處理、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-089 PAR2 建立

| 欄位 | 內容 |
|------|------|
| **用途** | 建立 PAR2 修復檔（用於修復損壞檔案） |
| **技術重點** | Reed-Solomon 編碼、冗餘區塊 |
| **WASM 模組** | par2-wasm (~500 KB) |
| **執行流程** | 1. 選擇檔案 → 2. 設定冗餘比例 → 3. 建立 PAR2 → 4. 下載 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | PAR2 |
| **效能需求** | 記憶體：檔案大小 x冗餘比、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-090 PAR2 修復

| 欄位 | 內容 |
|------|------|
| **用途** | 使用 PAR2 檔案修復損壞檔案 |
| **技術重點** | Reed-Solomon 解碼、錯誤修正 |
| **WASM 模組** | par2-wasm (~500 KB) |
| **執行流程** | 1. 上傳損壞檔案 + PAR2 → 2. 驗證 → 3. 修復 → 4. 下載 |
| **輸入格式** | 損壞檔案 + PAR2 |
| **輸出格式** | 修復後檔案 |
| **效能需求** | 記憶體：檔案大小 x2、處理時間：依損壞程度 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-091 檔案重複偵測

| 欄位 | 內容 |
|------|------|
| **用途** | 從多個檔案中找出重複檔案 |
| **技術重點** | 大小篩選、雜湊比對、分群顯示 |
| **WASM 模組** | 原生 File API (無需 WASM) |
| **執行流程** | 1. 上傳多檔案 → 2. 分析重複 → 3. 顯示結果 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | 重複檔案報告 |
| **效能需求** | 記憶體：依檔案數量、處理時間：依數量 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-092 檔案名稱批量修改

| 欄位 | 內容 |
|------|------|
| **用途** | 批量重新命名檔案 |
| **技術重點** | 命名規則、序號、正則替換 |
| **WASM 模組** | 原生 File API (無需 WASM) |
| **執行流程** | 1. 上傳多檔案 → 2. 設定命名規則 → 3. 預覽 → 4. 打包下載 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | ZIP (重命名後) |
| **效能需求** | 記憶體：檔案總和、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-093 資料夾結構建立

| 欄位 | 內容 |
|------|------|
| **用途** | 根據規則建立資料夾結構壓縮檔 |
| **技術重點** | 目錄範本、空目錄建立 |
| **WASM 模組** | zip-wasm (~800 KB) |
| **執行流程** | 1. 設定目錄結構 → 2. 建立 → 3. 下載 ZIP |
| **輸入格式** | 目錄結構描述 |
| **輸出格式** | ZIP |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-094 檔案類型篩選

| 欄位 | 內容 |
|------|------|
| **用途** | 從壓縮檔中篩選特定類型檔案 |
| **技術重點** | 副檔名篩選、MIME 類型、正則匹配 |
| **WASM 模組** | libarchive-wasm (~3 MB) |
| **執行流程** | 1. 上傳壓縮檔 → 2. 設定篩選條件 → 3. 提取 → 4. 下載 |
| **輸入格式** | ZIP, 7Z, RAR |
| **輸出格式** | ZIP (篩選後) |
| **效能需求** | 記憶體：依篩選結果、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-095 壓縮檔搜尋

| 欄位 | 內容 |
|------|------|
| **用途** | 在壓縮檔中搜尋檔案名稱 |
| **技術重點** | 目錄搜尋、模糊匹配、路徑顯示 |
| **WASM 模組** | libarchive-wasm (~3 MB) |
| **執行流程** | 1. 上傳壓縮檔 → 2. 輸入搜尋關鍵字 → 3. 顯示結果 |
| **輸入格式** | ZIP, 7Z, RAR |
| **輸出格式** | 搜尋結果列表 |
| **效能需求** | 記憶體：低、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-096 壓縮檔內容搜尋

| 欄位 | 內容 |
|------|------|
| **用途** | 在壓縮檔內的文字檔案中搜尋內容 |
| **技術重點** | 解壓搜尋、文字檔偵測、關鍵字高亮 |
| **WASM 模組** | libarchive-wasm (~3 MB) |
| **執行流程** | 1. 上傳壓縮檔 → 2. 輸入搜尋文字 → 3. 顯示匹配結果 |
| **輸入格式** | ZIP |
| **輸出格式** | 搜尋結果 |
| **效能需求** | 記憶體：解壓後大小、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-097 Base64 檔案編碼

| 欄位 | 內容 |
|------|------|
| **用途** | 將檔案編碼為 Base64 文字 |
| **技術重點** | Base64 編碼、分行處理 |
| **WASM 模組** | 原生 btoa API (無需 WASM) |
| **執行流程** | 1. 上傳檔案 → 2. 編碼 → 3. 複製/下載 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | Base64 文字 |
| **效能需求** | 記憶體：檔案大小 x1.4、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-098 Base64 檔案解碼

| 欄位 | 內容 |
|------|------|
| **用途** | 將 Base64 文字還原為檔案 |
| **技術重點** | Base64 解碼、格式偵測 |
| **WASM 模組** | 原生 atob API (無需 WASM) |
| **執行流程** | 1. 貼上 Base64 → 2. 解碼 → 3. 下載檔案 |
| **輸入格式** | Base64 文字 |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：文字大小 x0.75、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-099 Hex 檔案檢視

| 欄位 | 內容 |
|------|------|
| **用途** | 以十六進位檢視檔案內容 |
| **技術重點** | Hex 顯示、ASCII 對照、偏移量 |
| **WASM 模組** | 原生 (無需 WASM) |
| **執行流程** | 1. 上傳檔案 → 2. 顯示 Hex 內容 → 3. 搜尋/跳轉 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | Hex 顯示 |
| **效能需求** | 記憶體：分頁載入、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-100 檔案格式識別

| 欄位 | 內容 |
|------|------|
| **用途** | 識別檔案的真實格式（Magic Number） |
| **技術重點** | 檔案簽名、Magic Number 資料庫 |
| **WASM 模組** | 原生 (無需 WASM) |
| **執行流程** | 1. 上傳檔案 → 2. 分析簽名 → 3. 顯示格式 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | 格式資訊 |
| **效能需求** | 記憶體：極低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## 下一個檔案

CMP-101 ~ CMP-120 請參閱 [plan3-6.md](plan3-6.md)
