# Plan 3-2: 壓縮轉檔工具 (CMP-021 ~ CMP-040)

> 共 120 個工具，本檔案包含 CMP-021 ~ CMP-040

---

## CMP-021 XZ/LZMA 解壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 解壓縮 XZ 檔案 |
| **技術重點** | LZMA2 解壓 |
| **WASM 模組** | xz-wasm (~600 KB) |
| **執行流程** | 1. 上傳 .xz → 2. 解壓 → 3. 下載 |
| **輸入格式** | XZ |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：壓縮檔大小 x4、處理時間：中等 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-022 Zstandard 壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 將檔案壓縮為 Zstd 格式（快速高壓縮比） |
| **技術重點** | Zstd 壓縮、壓縮等級 1-22 |
| **WASM 模組** | zstd-wasm (~500 KB) |
| **執行流程** | 1. 選擇檔案 → 2. 設定等級 → 3. 壓縮 → 4. 下載 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | ZST |
| **效能需求** | 記憶體：檔案大小 x2、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-023 Zstandard 解壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 解壓縮 Zstd 檔案 |
| **技術重點** | Zstd 解壓、串流處理 |
| **WASM 模組** | zstd-wasm (~500 KB) |
| **執行流程** | 1. 上傳 .zst → 2. 解壓 → 3. 下載 |
| **輸入格式** | ZST |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：壓縮檔大小 x3、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-024 Brotli 壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 將檔案壓縮為 Brotli 格式（網頁優化） |
| **技術重點** | Brotli 壓縮、品質等級 0-11 |
| **WASM 模組** | brotli-wasm (~400 KB) |
| **執行流程** | 1. 選擇檔案 → 2. 設定品質 → 3. 壓縮 → 4. 下載 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | BR |
| **效能需求** | 記憶體：檔案大小 x2、處理時間：依等級 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-025 Brotli 解壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 解壓縮 Brotli 檔案 |
| **技術重點** | Brotli 解壓 |
| **WASM 模組** | brotli-wasm (~400 KB) |
| **執行流程** | 1. 上傳 .br → 2. 解壓 → 3. 下載 |
| **輸入格式** | BR |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：壓縮檔大小 x3、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-026 LZ4 壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 將檔案壓縮為 LZ4 格式（極速壓縮） |
| **技術重點** | LZ4 壓縮、區塊大小設定 |
| **WASM 模組** | lz4-wasm (~200 KB) |
| **執行流程** | 1. 選擇檔案 → 2. 壓縮 → 3. 下載 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | LZ4 |
| **效能需求** | 記憶體：檔案大小 x2、處理時間：極快 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-027 LZ4 解壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 解壓縮 LZ4 檔案 |
| **技術重點** | LZ4 解壓 |
| **WASM 模組** | lz4-wasm (~200 KB) |
| **執行流程** | 1. 上傳 .lz4 → 2. 解壓 → 3. 下載 |
| **輸入格式** | LZ4 |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：壓縮檔大小 x3、處理時間：極快 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-028 Snappy 壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 將檔案壓縮為 Snappy 格式（Google 開發） |
| **技術重點** | Snappy 壓縮、速度優先 |
| **WASM 模組** | snappy-wasm (~100 KB) |
| **執行流程** | 1. 選擇檔案 → 2. 壓縮 → 3. 下載 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | SNZ |
| **效能需求** | 記憶體：檔案大小 x2、處理時間：極快 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-029 Snappy 解壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 解壓縮 Snappy 檔案 |
| **技術重點** | Snappy 解壓 |
| **WASM 模組** | snappy-wasm (~100 KB) |
| **執行流程** | 1. 上傳 Snappy → 2. 解壓 → 3. 下載 |
| **輸入格式** | SNZ |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：壓縮檔大小 x3、處理時間：極快 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-030 LZO 壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 將檔案壓縮為 LZO 格式 |
| **技術重點** | LZO 壓縮、即時解壓優化 |
| **WASM 模組** | lzo-wasm (~150 KB) |
| **執行流程** | 1. 選擇檔案 → 2. 壓縮 → 3. 下載 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | LZO |
| **效能需求** | 記憶體：檔案大小 x2、處理時間：極快 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-031 LZO 解壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 解壓縮 LZO 檔案 |
| **技術重點** | LZO 解壓 |
| **WASM 模組** | lzo-wasm (~150 KB) |
| **執行流程** | 1. 上傳 .lzo → 2. 解壓 → 3. 下載 |
| **輸入格式** | LZO |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：壓縮檔大小 x3、處理時間：極快 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-032 萬用解壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 自動識別並解壓各種壓縮格式 |
| **技術重點** | 格式偵測、多模組載入、統一介面 |
| **WASM 模組** | libarchive-wasm (~3 MB) |
| **執行流程** | 1. 上傳壓縮檔 → 2. 自動偵測格式 → 3. 解壓 → 4. 下載 |
| **輸入格式** | ZIP, 7Z, RAR, TAR, GZ, BZ2, XZ, ZST |
| **輸出格式** | 原始檔案 |
| **效能需求** | 記憶體：依格式、處理時間：依格式 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-033 壓縮格式轉換

| 欄位 | 內容 |
|------|------|
| **用途** | 將壓縮檔轉換為其他壓縮格式 |
| **技術重點** | 解壓 + 重壓、格式選擇 |
| **WASM 模組** | libarchive-wasm (~3 MB) |
| **執行流程** | 1. 上傳壓縮檔 → 2. 選擇目標格式 → 3. 轉換 → 4. 下載 |
| **輸入格式** | ZIP, 7Z, TAR.GZ |
| **輸出格式** | ZIP, 7Z, TAR.GZ |
| **效能需求** | 記憶體：解壓後大小 x2、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-034 批量壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 批量壓縮多個檔案（每個獨立壓縮） |
| **技術重點** | 並行處理、進度追蹤、統一格式 |
| **WASM 模組** | zip-wasm (~800 KB) |
| **執行流程** | 1. 選擇多檔案 → 2. 設定格式 → 3. 批量壓縮 → 4. 打包下載 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | ZIP (多個) |
| **效能需求** | 記憶體：依批量大小、處理時間：並行加速 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-035 批量解壓縮

| 欄位 | 內容 |
|------|------|
| **用途** | 批量解壓多個壓縮檔 |
| **技術重點** | 並行處理、格式自動偵測 |
| **WASM 模組** | libarchive-wasm (~3 MB) |
| **執行流程** | 1. 上傳多個壓縮檔 → 2. 批量解壓 → 3. 打包下載 |
| **輸入格式** | ZIP, 7Z, RAR, TAR.GZ |
| **輸出格式** | ZIP (解壓後所有檔案) |
| **效能需求** | 記憶體：依批量大小、處理時間：並行加速 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-036 壓縮檔預覽

| 欄位 | 內容 |
|------|------|
| **用途** | 預覽壓縮檔內容而不解壓 |
| **技術重點** | 目錄解析、檔案列表、大小顯示 |
| **WASM 模組** | libarchive-wasm (~3 MB) |
| **執行流程** | 1. 上傳壓縮檔 → 2. 解析目錄 → 3. 顯示內容列表 |
| **輸入格式** | ZIP, 7Z, RAR, TAR |
| **輸出格式** | 目錄列表顯示 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-037 選擇性解壓

| 欄位 | 內容 |
|------|------|
| **用途** | 只解壓壓縮檔中選定的檔案 |
| **技術重點** | 檔案選擇、部分解壓、路徑處理 |
| **WASM 模組** | libarchive-wasm (~3 MB) |
| **執行流程** | 1. 上傳壓縮檔 → 2. 選擇檔案 → 3. 解壓選定 → 4. 下載 |
| **輸入格式** | ZIP, 7Z, RAR |
| **輸出格式** | 選定的檔案 |
| **效能需求** | 記憶體：選定檔案大小、處理時間：依選定量 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-038 壓縮檔修復

| 欄位 | 內容 |
|------|------|
| **用途** | 嘗試修復損壞的壓縮檔 |
| **技術重點** | 錯誤偵測、部分恢復、CRC 修正 |
| **WASM 模組** | zip-wasm (~800 KB) |
| **執行流程** | 1. 上傳損壞壓縮檔 → 2. 分析損壞 → 3. 嘗試修復 → 4. 下載 |
| **輸入格式** | ZIP |
| **輸出格式** | ZIP (修復後) |
| **效能需求** | 記憶體：檔案大小 x2、處理時間：依損壞程度 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-039 壓縮檔註解

| 欄位 | 內容 |
|------|------|
| **用途** | 為壓縮檔添加或編輯註解 |
| **技術重點** | 註解欄位、多語言編碼 |
| **WASM 模組** | zip-wasm (~800 KB) |
| **執行流程** | 1. 上傳壓縮檔 → 2. 編輯註解 → 3. 儲存 → 4. 下載 |
| **輸入格式** | ZIP |
| **輸出格式** | ZIP (含註解) |
| **效能需求** | 記憶體：檔案大小、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CMP-040 檔案分割

| 欄位 | 內容 |
|------|------|
| **用途** | 將大檔案分割成多個小檔 |
| **技術重點** | 分割大小、自動命名、合併資訊 |
| **WASM 模組** | 原生 File API (無需 WASM) |
| **執行流程** | 1. 上傳大檔 → 2. 設定分割大小 → 3. 分割 → 4. 打包下載 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | ZIP (多個分割檔) |
| **效能需求** | 記憶體：分割大小、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## 下一個檔案

CMP-041 ~ CMP-060 請參閱 [plan3-3.md](plan3-3.md)
