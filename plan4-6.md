# Plan 4-6: 加密解密工具 (CRY-101 ~ CRY-120)

> 共 130 個工具，本檔案包含 CRY-101 ~ CRY-120

---

## CRY-101 Diffie-Hellman 金鑰交換

| 欄位 | 內容 |
|------|------|
| **用途** | 展示 DH 金鑰交換原理 |
| **技術重點** | DH 參數、公私鑰生成、共享秘密計算 |
| **WASM 模組** | crypto-wasm (~500 KB) |
| **執行流程** | 1. 生成參數 → 2. 雙方生成金鑰 → 3. 計算共享秘密 |
| **輸入格式** | DH 參數 |
| **輸出格式** | 公鑰、共享秘密 |
| **效能需求** | 記憶體：低、處理時間：數秒 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-102 ECDH 金鑰交換

| 欄位 | 內容 |
|------|------|
| **用途** | 展示 ECDH 金鑰交換 |
| **技術重點** | 橢圓曲線 DH、P-256/X25519 |
| **WASM 模組** | nacl-wasm (~300 KB) |
| **執行流程** | 1. 選擇曲線 → 2. 生成金鑰 → 3. 計算共享秘密 |
| **輸入格式** | 對方公鑰 |
| **輸出格式** | 公鑰、共享秘密 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-103 ASN.1 解碼器

| 欄位 | 內容 |
|------|------|
| **用途** | 解碼並檢視 ASN.1 結構 |
| **技術重點** | DER/BER 解析、OID 識別、樹狀顯示 |
| **WASM 模組** | asn1-wasm (~300 KB) |
| **執行流程** | 1. 上傳檔案 → 2. 解碼 → 3. 顯示結構 |
| **輸入格式** | DER/BER/PEM |
| **輸出格式** | 結構化檢視 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-104 XOR 加密

| 欄位 | 內容 |
|------|------|
| **用途** | 使用 XOR 進行簡單加密（教學用） |
| **技術重點** | XOR 運算、重複金鑰 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入資料 → 2. 輸入金鑰 → 3. XOR → 4. 輸出 |
| **輸入格式** | 文字/檔案、金鑰 |
| **輸出格式** | XOR 結果 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-105 Caesar 密碼

| 欄位 | 內容 |
|------|------|
| **用途** | 凱撒密碼加密/解密（古典密碼學） |
| **技術重點** | 字母位移、ROT13 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入文字 → 2. 設定位移 → 3. 加密/解密 |
| **輸入格式** | 文字 |
| **輸出格式** | 加密/解密文字 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-106 Vigenère 密碼

| 欄位 | 內容 |
|------|------|
| **用途** | 維吉尼亞密碼加密/解密 |
| **技術重點** | 多字母替換、關鍵字 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入文字 → 2. 輸入關鍵字 → 3. 加密/解密 |
| **輸入格式** | 文字、關鍵字 |
| **輸出格式** | 加密/解密文字 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-107 Playfair 密碼

| 欄位 | 內容 |
|------|------|
| **用途** | Playfair 密碼加密/解密 |
| **技術重點** | 5x5 矩陣、雙字母替換 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入文字 → 2. 輸入關鍵字 → 3. 加密/解密 |
| **輸入格式** | 文字、關鍵字 |
| **輸出格式** | 加密/解密文字 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-108 Enigma 模擬器

| 欄位 | 內容 |
|------|------|
| **用途** | 模擬 Enigma 加密機 |
| **技術重點** | 轉子配置、反射器、插板 |
| **WASM 模組** | enigma-wasm (~200 KB) |
| **執行流程** | 1. 設定轉子 → 2. 設定起始位置 → 3. 輸入訊息 → 4. 加密 |
| **輸入格式** | 文字、機器設定 |
| **輸出格式** | 加密文字 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-109 摩斯密碼

| 欄位 | 內容 |
|------|------|
| **用途** | 文字與摩斯密碼互轉 |
| **技術重點** | 國際摩斯碼、音訊播放 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入文字/摩斯碼 → 2. 轉換 → 3. 播放音訊 |
| **輸入格式** | 文字或摩斯碼 |
| **輸出格式** | 摩斯碼或文字、音訊 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-110 Atbash 密碼

| 欄位 | 內容 |
|------|------|
| **用途** | Atbash 替換密碼 |
| **技術重點** | 字母反轉替換（A↔Z） |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入文字 → 2. 轉換 → 3. 輸出 |
| **輸入格式** | 文字 |
| **輸出格式** | 轉換文字 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-111 Rail Fence 密碼

| 欄位 | 內容 |
|------|------|
| **用途** | Rail Fence 柵欄密碼 |
| **技術重點** | 鋸齒形排列、軌道數 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入文字 → 2. 設定軌道數 → 3. 加密/解密 |
| **輸入格式** | 文字 |
| **輸出格式** | 加密/解密文字 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-112 Bacon 密碼

| 欄位 | 內容 |
|------|------|
| **用途** | 培根密碼（隱寫術） |
| **技術重點** | 二進位編碼、字型隱藏 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入訊息 → 2. 輸入載體文字 → 3. 編碼/解碼 |
| **輸入格式** | 秘密訊息、載體文字 |
| **輸出格式** | 隱藏訊息的文字 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-113 金鑰熵分析

| 欄位 | 內容 |
|------|------|
| **用途** | 分析金鑰或資料的熵值 |
| **技術重點** | Shannon 熵、位元組分佈、隨機性評估 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入資料 → 2. 計算熵 → 3. 顯示分析結果 |
| **輸入格式** | 文字、十六進位、檔案 |
| **輸出格式** | 熵值、分佈圖 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-114 頻率分析

| 欄位 | 內容 |
|------|------|
| **用途** | 分析文字字母頻率（密碼分析） |
| **技術重點** | 字母統計、與標準頻率比較 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入密文 → 2. 統計頻率 → 3. 顯示圖表 |
| **輸入格式** | 文字 |
| **輸出格式** | 頻率圖表、統計資料 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-115 雜湊比較器

| 欄位 | 內容 |
|------|------|
| **用途** | 比較兩個雜湊值是否相同 |
| **技術重點** | 時間恆定比較、格式統一 |
| **WASM 模組** | 原生 JS (無需 WASM) |
| **執行流程** | 1. 輸入雜湊 A → 2. 輸入雜湊 B → 3. 比較結果 |
| **輸入格式** | 雜湊值（任意格式） |
| **輸出格式** | 比較結果 |
| **效能需求** | 記憶體：低、處理時間：即時 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-116 檔案雜湊批次計算

| 欄位 | 內容 |
|------|------|
| **用途** | 批次計算多檔案雜湊值 |
| **技術重點** | 多檔案處理、多演算法、匯出報告 |
| **WASM 模組** | hash-wasm (~200 KB) |
| **執行流程** | 1. 上傳多檔案 → 2. 選擇演算法 → 3. 計算 → 4. 匯出 |
| **輸入格式** | 多個檔案 |
| **輸出格式** | 雜湊報告 (CSV/JSON) |
| **效能需求** | 記憶體：依檔案數、處理時間：依總大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-117 雜湊檔案驗證

| 欄位 | 內容 |
|------|------|
| **用途** | 使用校驗檔驗證檔案完整性 |
| **技術重點** | MD5SUM/SHA256SUM 格式、批次驗證 |
| **WASM 模組** | hash-wasm (~200 KB) |
| **執行流程** | 1. 上傳檔案 → 2. 上傳校驗檔 → 3. 驗證 → 4. 顯示結果 |
| **輸入格式** | 檔案、校驗檔 |
| **輸出格式** | 驗證結果 |
| **效能需求** | 記憶體：依檔案大小、處理時間：依大小 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-118 CSPRNG 測試

| 欄位 | 內容 |
|------|------|
| **用途** | 測試隨機數生成器品質 |
| **技術重點** | NIST 測試、卡方檢定、連續性測試 |
| **WASM 模組** | rng-test-wasm (~400 KB) |
| **執行流程** | 1. 上傳隨機資料 → 2. 執行測試 → 3. 顯示報告 |
| **輸入格式** | 隨機資料（二進位） |
| **輸出格式** | 測試報告 |
| **效能需求** | 記憶體：中等、處理時間：較長 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-119 時間戳簽章

| 欄位 | 內容 |
|------|------|
| **用途** | 為檔案建立時間戳記證明 |
| **技術重點** | TSA 概念、本地時間戳、雜湊記錄 |
| **WASM 模組** | crypto-wasm (~500 KB) |
| **執行流程** | 1. 上傳檔案 → 2. 計算雜湊 → 3. 建立時間戳 → 4. 下載證明 |
| **輸入格式** | 任意檔案 |
| **輸出格式** | 時間戳證明 (JSON) |
| **效能需求** | 記憶體：低、處理時間：快速 |
| **進度狀態** | ⬜ 待開發 |

---

## CRY-120 加密演算法比較

| 欄位 | 內容 |
|------|------|
| **用途** | 比較不同加密演算法的效能 |
| **技術重點** | 多演算法基準測試、吞吐量測量 |
| **WASM 模組** | 多個加密模組 |
| **執行流程** | 1. 選擇演算法 → 2. 設定資料量 → 3. 執行測試 → 4. 顯示比較 |
| **輸入格式** | 測試參數 |
| **輸出格式** | 效能比較報告 |
| **效能需求** | 記憶體：依測試、處理時間：較長 |
| **進度狀態** | ⬜ 待開發 |

---

## 下一個檔案

CRY-121 ~ CRY-130 請參閱 [plan4-7.md](plan4-7.md)
